# Universe V2 架构迁移说明（进行中）

## 1. 本轮已经替换的基础设施

| 旧实现（V1） | V2 实现 |
|---|---|
| 太空以 `enteredAt` 为原点，每次入太空围绕玩家重摆宇宙 | `src/space/universe.js`：恒星系统坐标原点为恒星，天体位置独立于玩家 |
| 1 个游戏单位同时被解释为 1 m 和 149,597 km | 宇宙逻辑层全部使用真实米制（double）；渲染层使用浮动原点 + 对数深度缓冲 |
| 行星视觉球 + 完全无关的平面体素地图 | 每个天体有固定地表锚点；`surfaceToUniverse/universeToSurface` 双向映射，起飞点/落点/太阳方向同源 |
| `speed` 标量 + `vertVel` 独立升降；W 永远水平 | `ShipFlight.vel` 是真实三维速度矢量；W 沿机头方向（含 pitch）推进 |
| 月球/金星放在玩家旁边 | 月球真实 384,400 km；行星真实 AU 距离；脉冲速度包线解决旅行时间 |
| 跨星球 = 接近标记 → 1 秒闪光 → 固定出生点 (8.5, +300, 8.5) | 接近另一颗可登陆天体且速度 < 800 m/s 时，连续转换到该天体地表帧；落点由真实宇宙位置反算 |
| 地表太阳与太空太阳两套实现 | `starDirectionM()` 由恒星位置计算；地表天空、太空方向光、昼夜、星图共用 |
| 星图是脱离玩家的示意图 | 系统级星图投影真实玩家位置；距离读取 Universe；行星系统图按真实卫星方向/轨道比例绘制 |
| 存档不保存飞船空间状态 | 保存 `flight` 速度/姿态/宇宙坐标/太空状态并恢复 |

## 2. 关键坐标关系

```
局部体素世界 (x=东, y=上, z=北, 1 u ≈ 1 m)
        │  surfaceToUniverse（含天体自转 spin 与地表锚点）
        ▼
天体固连球面 (lat/lon) → 天体中心 + (R + altitude)·up
        │  + body.posM
        ▼
恒星系统宇宙坐标（真实米，double）
        │  渲染时：renderPos = posM - cameraOrigin
        ▼
THREE 场景（Float32 安全，对数深度，camera.far 动态扩展到全系统）
```

## 3. 飞船航行参数（游戏化，但速度是真实连续的）

- 大气层：基础最大 220 m/s，引擎每级 +60 m/s；W 推力 16 m/s²，S 制动 22 m/s²。
- 大气层顶：稠密大气 40 km / 稀薄 30 km / 无大气 20 km（游戏化高度，连续过渡）。
- 深空脉冲：速度包线 `v = sqrt(2·a·h)`，`a = 2,340,000 m/s²`，最后 20,000 km 线性收束；
  接近天体时自动连续制动，交接速度 < 800 m/s，然后在高空继续制动到大气速度上限。
- 实测（纯逻辑模拟）：地球 → 月球约 62 秒，中途最高约 0.098 c。
- 跨恒星系统仍通过跃迁门（门内为真实空间，穿越为跃迁）。

## 4. 当前阶段覆盖范围

- 已打通样板链路：**地球地表 → 大气层 → 近地空间 → 地月空间 → 月球地表**。
- 其他岩质行星/卫星使用同一套连续转换逻辑；气态巨行星不再允许体素地表降落。
- 空间站/小行星带/跃迁门已迁移到 Universe 坐标。

## 5. 尚未完成（后续阶段）

1. 行星公转/卫星绕转的轨道时钟（当前轨道静态）。
2. 地表块坐标跨 360° 环绕与球面区块分页（当前用锚点 + 经度周期保持连续）。
3. 气态巨行星、恒星、小行星的差异化接近玩法。
4. 恒星级地图与多星系真实尺度（仍为门跃迁）。
5. 浏览器回归套件适配（当前 Node 纯逻辑测试：`npm run test` 与 `npm run test:v2` 全绿）。

## 6. 回归状态

- `node tests/run.js`：44677 通过，0 失败。
- `node tests/run-v2.js`：32 通过，0 失败。
