# 开发进度（Goal 轮次记录）

## 修复：世界渲染"凌乱零碎/尖刺森林"（体素网格拓扑灾难）
- **现象**：用户截图显示全场景几何体撕裂成拉伸尖刺（HUD/任务正常）——经无头浏览器几何审计确认：全区 363 个网格含 **28066 条超长三角形边**（合法上限 √3≈1.73）+ 10556 个退化三角形，每个区块无一幸免。
- **根因**：`src/world/mesher.js` 每个方块面/植物十字面只发射 **4 个顶点（一个四边形）**，但 `BufferGeometry` **没有索引缓冲**。非索引几何按"每 3 个连续顶点 = 1 个三角形"渲染 → 每面的第 2 个三角形由「上一个面的最后 1 顶点 + 下一个面的前 2 顶点」拼成，横跨两个不相干的方块面 → 满屏拉伸尖刺。
- **修复**：四边形改为**索引发射**（4 顶点/面 + 6 索引/面 `b,b+1,b+2, b,b+2,b+3`，CCW），`geo.setIndex(Uint32Array)`（防单区块 >65535 顶点）；`toMeshes` 无需其他改动。
- **举一反三（防回归）**：
  1. `tests/run.js` 新增**拓扑审计**：索引数 = 顶点数/4×6、每个三角形 3 索引必须落在同一 4 顶点四边形窗口、所有边长 ≤ √3+ε、无退化三角形（不透明/镂空两缓冲都查）——此类 bug 从此无法通过测试。
  2. 新增 `tools/reproworld.mjs`：浏览器级全场景几何审计（按索引缓冲遍历三角形，报告超长边/退化/NaN）+ 高分屏截图，可随时复检。
  3. 排查全库其余自建 BufferGeometry：采矿光束（Line，2 顶点）、星空（Points）、粒子（InstancedMesh+BoxGeometry）均为非三角形发射或自带索引，无同类隐患。
- **验证**：修复后审计 **超长边 0 / 退化 0 / 坏网格 0**，299194 个三角形全部合法；2560×1600 截图视觉确认：方块地形/植物/贴图全部正常。`node tests/run.js`：**44190 断言全过**（含新增拓扑审计）。

## 第 5 轮（已完成 · 目标达成）
- **任务链补全（12 步）**：插入"制作多功能工具"与"建造简易庇护所"（放 12 方块）两步，`quests.onPlace` 接线；全部完成后弹出**旅程完成面板**（行星数/任务 12/12/当前位置）
- **战斗系统** `src/entities/mobs.js` + `src/entities/combat.js`：夜间异星生物（追击/攻击/白天燃烧/死亡掉落碳或钠/受击闪烁/腿部摆动动画），Q 键能量光束（弹道+尾迹+生物/方块命中判定）
- **洞穴暗化**：`sky.caveDim(k)`（日光 87% 压低+雾收缩）+ 头灯 PointLight（洞穴内自动点亮 2.6）
- 新音效：blaster/mobHurt/mobAttack/mobDie/mobSpawn

### 本轮修复
1. 战斗测试瞄准反了：相机俯仰约定"正值向上"（pitch -= md.y），测试用 atan2 正值 → 改为负值
2. 生物 30 血 = 3 发致死（测试循环补足）
3. 旅程面板 setTimeout 调用 g.showJourney → 测试假 game 需提供该方法

### 验证（全部通过）
- `node tests/run.js`：**44180 断言**（12 步任务链状态机/存档往返/星系/合成）
- `node tools/round5.mjs`：夜间生物生成 ✓、3 发弹道击杀+掉落碳×2 ✓、洞穴阳光 0.010 vs 地表 0.08 + 头灯 2.58 ✓、旅程面板 12/12 ✓
- `node tools/mobview.mjs` + 视觉确认：深红方块生物+橙色发光眼 ✓
- 全量回归：tests + screenshot + flow + flight + space + scan + round4 + round5 + perf 全部 0 页面错误；初始化 342ms、帧率 194–238

## 目标验收（对照原始要求逐项达成）
- 前期流程 1:1（坠毁→采集→工具→庇护所→修船→起飞→太空→跨星球）✅ 12 步任务链
- 生存属性（生命/氧气/护盾）+危险防护 ✅；背包 ✅；合成 ✅；工具/武器 ✅；方块建造破坏 ✅；地形矿物 ✅；昼夜 ✅；扫描采集 ✅；飞船驾驶 ✅；星际航行 ✅；星球探索 ✅
- 画风融合（体素+像素纹理+科幻太空/星云/粒子）✅；半透明科幻 HUD/扫描脉冲/星图/背包/合成界面 ✅；光影雾效星空天空盒 ✅
- 音效全清单 ✅（全 WebAudio 程序合成，原创素材）；交互清单（准星高亮/悬停点击/震动/粒子）✅
- 可运行完整 Demo ✅（start.bat，存档系统，44k+ 断言，8 个浏览器验证脚本）

## 第 4 轮（已完成）
- **洞穴生存**：`world.isUnderground`（头顶覆盖统计 + 深度阈值 ≥3，树叶不计入，树冠/浅棚不误判）；洞穴内氧气 2.2/s 消耗、耗尽扣血、<25 警告音+提示；地表 7/s 回复
- **洞穴环境音**：`audio.setCaveMode`（风声压低至 0.02 + 1.8s 间隔随机滴水双音）
- **多功能工具**：新瓦片 MULTITOOL（27 号）+ 配方（金属镀层×3+碳×4）+ 挖掘 3.2 倍速 + 采矿光束（Line 抖动能量线，AdditiveBlending）
- **存档系统** `src/systems/save.js`：collect/apply/save/load/clear，60s 自动存档 + beforeunload；main.js 无 seed 参数时自动读档；菜单"继续游戏/开始新游戏"按钮（setupMenu）
- **云层**：24 朵程序云精灵（径向渐变团块），大气层内飘移+环绕玩家，太空淡出

### 本轮修复
1. URL 带 seed 参数时跳过存档 → 继续按钮改为直接进入（不再 reload 死循环）
2. 存档测试断言结构错误（player 扁平结构）→ 修正
3. 飞行时 toast 与飞行 HUD 重叠 → body.piloting 类上移 toast

### 验证
- `node tests/run.js`：44175 断言全过（新增存档往返/多功能工具配方测试）
- `node tools/round4.mjs`：洞穴氧气 96.96→92.56（2s，2.2/s ✓）→ 地表回复 100 ✓；工具 3.2 倍速 ✓；存档写→重载→背包 13 石头/坐标/种子完整恢复 ✓
- `node tools/saveui.mjs`：同会话第二页显示"继续/新游戏"按钮、点击继续直接进入游戏（背包 7 石头 ✓）
- `node tools/caveview.mjs`：洞穴内 underground=true、氧气持续下降
- 云层截图确认可见

## 第 3 轮（已完成）
- **星系系统** `src/space/system.js`：6 颗程序行星（确定性；名称/7 种类型/色板/半径/星系坐标），母星种子=游戏种子
- **太空场景**：170m 突破大气层 → 黑天+星空+4 片星云精灵、脚下巨行星（球面程序纹理+极冠+赤道带+微弱自发光+大气层辉光 fresnel 着色器）、邻居行星可见球体+名称标记（随距离缩放）
- **星际航行**：太空脉冲引擎 420 u/s、目标罗盘（相对角度+距离）、星图 UI（M 键：行星列表/类型/探索状态/设定目标）、跃迁闪屏+音效
- **跨星球跃迁**：接近目标行星自动跃迁 → applyNewWorld 换世界（新地形种子 + 植被 hue-rotate 色板 + 天空色调）→ 高空继续飞行 → 降落新星球；`world.setMeshesVisible` 太空隐藏地表、`sky.setSpaceMode/setPalette`
- **扫描系统** `src/entities/scanning.js`：C 键球形脉冲波 + 42 个资源点菱形标记（穿透地形、渐入淡出、轻微浮动）+ 冷却
- **任务链完结**：10 步全流程（…→起飞→飞入太空→探索另一颗星球→完成），达成"跨星球探索"
- **UI/交互**：星图面板、罗盘、跃迁闪屏、飞行时 toast 上移、座舱边框遮罩

### 本轮修复
1. 行星名 `h >> 5` 有符号位移负索引 → "行星 Xyundefined-527" → `>>> 5`
2. `this.warpFlash` 属性覆盖同名方法 → 重命名 `warpFlashEl`
3. 脉冲球 FrontSide 从内部不可见 → DoubleSide
4. 标记初始投影在视锥外/被地形遮挡 → depthTest:false 透视显示
5. 脚下行星背光面全黑 → 材质自发光 0.32

### 验证
- `node tests/run.js`：44155 断言全过（新增星系确定性/行星数据/命名格式/任务 9-10 步）
- `node tools/space.mjs`：起飞→太空（alt 170，地表隐藏，5 标记）→星图→罗盘→脉冲→跃迁→新星球降落→**任务 10 步全部完成**
- `node tools/spaceview.mjs`：行星+大气辉光可见、邻居星球球体可见、新行星地形颜色改变（像素均值验证）
- `node tools/scan.mjs` + 像素探针：脉冲 25% 屏幕覆盖、标记渲染 2.9 万青色像素
- 全部脚本 0 页面错误

## 第 2 轮（已完成）
- **合成系统** `src/systems/crafting.js`：7 条配方（金属镀层/密封胶/二氢凝胶/发射燃料/板材/玻璃/碳），材料检查与缺口计算，纯逻辑可测试
- **背包界面**：Tab/I 打开，36 格网格 + 合成面板（红绿材料计数、可用/禁用按钮、悬停音效、点击选中/交换快捷栏），打开时暂停模拟
- **飞船维修** `src/entities/ship.js`：脉冲引擎（金属镀层+密封胶）与发射推进器（发射燃料）部件状态，修复后引擎辉光（青/暖橙）+ 点光源，冒烟随修复停止
- **飞船驾驶** `src/entities/shipflight.js`：E 登船/离船、座舱视角（机身入画 + 座舱边框遮罩）、大气层飞行物理（油门/刹车/升降/鼠标转向/自动侧倾回正/悬停自动缓降）、山体碰撞保护、起降粒子、飞行 HUD（速度/高度/油门）
- **程序化引擎音效**：`audio.js` 引擎循环（油门联动滤波+音调）、起飞/降落/登船离船
- **任务链**：前 8 步全部可完成（…→制作镀层→修复引擎→补燃料→起飞），起飞触发 `quests.onLaunch()` 进入第 9 步"飞入太空"
- **新物品**：金属镀层/二氢凝胶/发射燃料/密封胶（4 个新像素瓦片）

### 第 2 轮修复
1. 悬停弹簧阻止降落 → 低速近地时自动缓降至触地
2. `groundHeight` 用未雕刻高度图 → 新增 `world.getGroundY()`（考虑弹坑雕刻），飞船起降/离船落点正确
3. 登船时船身嵌在弹坑壁 → 登船自动抬升至实际地表上方
4. 滑行阻力过小（0.5/s）→ 1.2/s，配合 S 刹车手感
5. 合成列表在 720p 截断 → 独立滚动区

## 第 1 轮（已完成）
（体素引擎、纹理、昼夜、坠机开场、玩家、挖掘建造、背包、音效、HUD 详见早期记录）

### 第 1 轮修复的关键 bug（经验记录）
1. AIR 方块 opacity 误设为 'opaque' → 所有暴露面被剔除
2. remeshQueue 网格化时创建邻居区块 → 脏集合边界无限扩张（初始化 35s → 0.3s）
3. 天空穹顶着色器 pow(负值) → NaN → 地平线以下纯黑
4. 纹理图集第二行瓦片 offset 计算错误 → 飞船外壳采样到草地行
5. 区块网格叠加世界偏移 → 地形错位碎散
6. ACES 色调映射压碎暗部 → 弹坑底面/焦土纯黑（提亮 AO/底面/补光/月光）

### 验证方法（tools/）
- `node tests/run.js`：纯逻辑测试
- `node tools/screenshot.js shots 1200`：无头 Chrome 截屏（菜单/出生点/飞船/挖掘/夜晚）
- `node tools/flow.mjs`：合成→维修全流程
- `node tools/flight.mjs`：登船→起飞→巡航→降落→离船
- `node tools/space.mjs`：太空全旅程（含跃迁）
- `node tools/spaceview.mjs`：太空视觉定向验证
- `node tools/scan.mjs`：扫描脉冲验证
- `node tools/round4.mjs`：洞穴氧气/工具/存档验证
- `node tools/saveui.mjs`：存档菜单流程验证
- `node tools/caveview.mjs`：洞穴内状态验证
- `node tools/perf.mjs`：初始化耗时与帧率
- 像素级验证：readPixels 直读颜色分布（vision 模型对体素画风易误读，以像素数据为准）

## 下一步（第 5 轮计划）
- **武器与战斗**：能量武器（光束枪）、敌对生物（夜间出没的异星生物）
- **画质打磨**：洞穴真实暗化（光照传播）、水、天气/沙暴粒子
- **更多内容**：更多配方/方块/生物群系、成就系统

## 第 7 轮（已完成）：操作手感 / 主菜单 / 设置 / 创造模式 / 飞船指引 / 草皮贴图修复

用户反馈的一批游玩问题全部修复，并按"举一反三"原则修复了同类潜在 bug：

### 修复清单
1. **放置消抖**：右键单击只放 1 个方块。根因：边沿事件（pressed/mousePressed）在固定步长循环（每帧最多 6 步）中被重复读取，单次点击最多放 6 个。→ Input 增加 consume/consumeMouse 消费语义，player 中所有边沿按键（放置/选取/跳跃/开火/E 互动/F/X 切换）用后即消费。
2. **0 步帧丢输入（同类隐患）**：高刷新率显示器上单帧 rawDt < 1/60 → 固定步为 0 → endFrame 整帧清空 pressedSet 导致按键偶发失灵。→ Input 改为"事件带帧龄、最多存活 2 帧"，0 步帧不丢输入、菜单态陈旧按键自动过期。
3. **视角偶发 180° 瞬转**：指针锁重入/浏览器异常时的超大鼠标增量无界进入视角。→ 单事件增量夹紧 ±450px + 重锁后丢弃前 2 个移动事件 + 帧级兜底夹紧（input.droppedJumps 计数可观测）。
4. **F 键=手电筒**（原为飞行开关）；飞行模式改为 **X 键**，且修复飞行无法下降（潜行键在飞行态被禁用 → Shift/Ctrl 均可下降，飞船驾驶同步支持）。
5. **游戏内 Esc 菜单**：继续游戏 / 保存游戏存档 / 设置 / 退出到主菜单（存档自动保存）。Esc 可关闭并返回。
6. **主菜单**：新游戏 / 继续游戏 / 读取存档 / 设置 / 退出游戏；进入游戏世界前可先调设置。
7. **设置面板**（主菜单与暂停菜单共用，localStorage 持久化）：视野 FOV 60–110、渲染距离 3–10、画面质量（低/中/高：像素比+阴影）、鼠标灵敏度 30%–200%、**创造/生存模式切换**。
8. **创造模式**：始终自由飞行（空格升/Shift 降）、全物品各一整组、放置不消耗、秒破方块、免疫伤害、生命/氧气/危险防护恒满。
9. **E 进飞船面板后 Esc 卡死**：根因是面板关闭后从 rAF 上下文 requestPointerLock（非用户手势会被浏览器拒绝）→ 游戏保持"未锁定+未暂停"的僵死态。→ requestGameLock 失败时置 wantLock，任何用户手势（按键/点击）自动重锁；超过 0.9s 未锁定弹出"点击屏幕继续"提示；暂停界面按任意键/Esc 可继续（resumeFromPause 清空滞留输入防误操作）。
10. **飞船残骸改版**：整体结构完整保留（机身/双翼/尾翼/引擎），局部损坏——脉冲引擎、**座舱玻璃碎裂（深色熏黑，修复后透亮）**、发射推进器燃油耗尽；新增散落船体板碎片；修复面板增加第 3 项"座舱玻璃"（强化玻璃×2+金属镀层）；任务链 12→13 步（repairPulse → repairGlass → fuelLaunch）。**修复船体半埋地下**：原按"地表高-弹坑深"放置导致船体陷入地面，改为按实际地表（getGroundY）落在弹坑之上；浓烟+余烬标记坠机点。
11. **飞船方位指引**：扫描（C）在飞船上方生成琥珀色大标记（24s）并播报方位距离文案；HUD 新增常驻"飞船罗盘"（距飞船 >9m 时显示 ▲+距离，起飞后隐藏）。
12. **第一人称激光枪**：右下角科幻激光枪视模型（枪身+青色能量核心+呼吸摆动+后坐），采矿光束从枪口发射（不再要求多功能工具，工具仅提速）；Q 开火不再需要多功能工具。
13. **草方块贴图 bug**：侧面纹理 4 向随机旋转（90°/180°/270°）把绿草条带翻到侧面/底部。→ 侧面只允许"竖直保持"（顶角永远取瓦片顶部 v，仅水平镜像）；顶/底面保留 4 向旋转。新增单方块精确回归测试（44204 项全部通过）。

### 验证
- `node tests/run.js`：**44204 通过，0 失败**（新增：草皮 UV 朝向、任务链 13 步、座舱玻璃材料、设置归一化）。
- `node tools/verifyround.mjs`：无头浏览器全流程验证（主菜单按钮/放置消抖/按键/0步帧存活/飞行下降/维修面板 Esc/暂停菜单/设置生效/创造秒破/飞船罗盘/玻璃修复/扫描标记/退出主菜单），全部通过，0 控制台错误；截图视觉确认：草地条带全部在顶、激光枪可见、飞船残骸整体可辨、菜单布局正常。


## 第 8 轮（已完成）：飞船视角 / 飞行手感 / 修复提示 / 任务同步

### 修复与优化
1. **飞船视角重做（双视角，V 键切换）**：
   - **座舱视角**：视线抬高到机身/座舱顶部之上（不再"趴在引擎盖上"），下半屏不再被船体遮挡；视野=机头方向+轻微俯仰跟随+少量反向滚转（地平线稳定）。
   - **第三人称追尾视角**：相机在船后上方平滑跟随（有惯性），完整看到整艘飞船飞行。
   - 视觉验证：座舱视角画面干净、地形可见；追尾视角整船可见。
2. **飞行手感物理化**：鼠标速度→期望角速度→角速度限幅+角加速度平滑（惯性转弯，无锐角折返）；**速度越快转向越慢**（低速转向率 0.48 rad/s vs 高速 0.26 rad/s，合理转向半径）；侧倾随转向率自动回正；加减速/升降保持惯性。
3. **修复状态提示**：飞船修好后靠近只提示"按 E 登上飞船"（历史 bug：仍提示修复）。
4. **任务与飞船真实状态自动同步**（`Quests.syncShip`）：
   - 组件已修复 → 对应任务立即标记完成（乱序修复/创造模式一次性修完，列表即时反映真实状态）；
   - 当前步骤恰好是已修复的维修步骤 → 自动推进（全修好直接同步到"起飞"）；
   - 停在材料类独立步骤时不被跳过，完成该步骤后自动跳过已完成的维修步骤直达起飞；
   - 挂接点：维修面板修复后、读档后、主循环每 12 帧兜底。
5. 星球同质化、太阳系/大飞船/空间站/NPC 等内容规划见下方"架构说明"与"下一步"。

### 架构说明（大白话版，供评估后续扩展）
- **太空与星球的关系**：整个游戏是一套"体素世界引擎"+"太空层"。地面就是一套 16×64×16 区块组成的体素世界（噪声地形+洞穴+矿脉+植被），飞船离地爬到 ~170 高度后从"地面模式"切换到"太空模式"：地面网格隐藏，换成程序生成的星球球体+星空+太阳（`src/space/system.js` 里的 `SpaceSystem`），你操纵的是同一个飞行物体，只是"参照系"换成了太空。
- **星图怎么组织**：星系 = 6 颗"数据星球"（`generateGalaxy`：类型/色板/名字/种子/轨道坐标都是按游戏种子确定性生成的纯数据）。每颗星球只是一个"参数包"——它的真实地表在你降落时用"星球自己的种子"重新生成一块体素世界（`applyNewWorld`）。所以"跨星球"本质是：记下 6 颗星的数据 → 飞行中切换到太空表现 → 降落时用目标星种子重建地表世界。星图面板（M 键）就是这 6 个参数包的列表，选定目标后 HUD 罗盘指向它，飞到附近即触发跃迁/降落。
- **扩展空间在哪**：
  - 星球数量/类型直接改 `PLANET_TYPES`/`generateGalaxy`（可换成太阳系 8 行星的固定配置+真实配色）；
  - 星球差异化：地表世界生成器（`chunk.js`）已支持按种子/色板扩展——后续加"地形参数化（重力/山高/洞穴密度）、专属方块与矿、天气、昼夜长度、生物群系"就能从"换草皮"变成"换星球"；
  - 空间站/NPC：太空层目前只有星球球体——可在轨道上放"空间站实体"（固定相对坐标的模型+交互半径），按 E 打开交易/任务面板；NPC=空间站内的 DOM 对话+商店数据表；
  - 大飞船/多星系：`SpaceSystem` 加"星系跳转"（目标星系=另一组 6 星参数包），大飞船=新的 ShipFlight 参数组（速度/货舱/燃料）。
- **星球同质化**：当前差异仅植被色相（palette.grassHue）。后续优先级：地形参数化 → 专属资源/方块 → 天气/环境危险 → 视觉主题。

### 验证
- `node tests/run.js`：**44212 通过，0 失败**（新增任务-飞船同步测试 5 项）。
- `node tools/verifyflight.mjs`：修复提示文案、任务同步三场景、座舱/追尾视角切换、转向限幅+惯性+高速转向变慢、离船恢复，全部通过，0 控制台错误；截图视觉确认两种视角正常。
- 上一轮 `tools/verifyround.mjs` 全量回归通过。

## 下一步（第 9 轮建议）
1. **太阳系化**：母星=地球风（默认草原/森林生态），金星/水星/火星等按真实特征做参数差异（灼热/荒漠/冰冻）。
2. **星球差异化内容**：地形参数化、专属方块/资源、天气与危险环境。
3. **太空实体**：空间站（交易/任务/造船）+ NPC 对话。
4. **多星系与大型飞船**：星系跳转、飞船等级（购买/制造）。

## 第 9 轮（已完成）：太阳系化 + 星球地表差异化（优先级 2 的第一项）

### 内容
1. **太阳系 8 行星**（`space/system.js` 的 `SOLAR_SYSTEM` 配置）：地球(繁茂家园/母星)·金星(剧毒温室)·水星(灼热荒岩)·火星(红色荒漠)·木星(气态巨行星)·土星(环带气态行星)·天王星/海王星(冰巨星)。轨道半径/星球大小按真实相对比例缩放，按游戏种子确定性排布。
2. **母星固定为地球**：绿色草地+森林（树密度 1.15）+蓝天，经典 MC 生存开局；菜单/加载文案同步改为"太阳系·地球"；HUD 星球名显示"地球 · 太阳系"（降落其他行星时显示对应名称）。
3. **星球地表差异化**（不再是换草皮颜色）：
   - 每颗行星带 `surface` 类型 + `terrain` 参数（基准高度/起伏幅度/山脉强度/洞穴密度/树木密度），WorldGen 按参数生成：火星红沙荒漠+高峰、水星荒岩、金星剧毒绿原、木星/土星橙黄气态层、天王星/海王星雪盖冻土、地球草地+海滩。
   - 新增方块 **红色沙地** 与 **雪层**（新图集瓦片，可挖掘/放置/掉落）。
4. **太空视觉增强**：太阳光球+日冕光晕；**土星环**（程序化环带纹理，邻居行星与母星视角均显示）。
5. **雪地纹理 bug 修复（举一反三）**：edgeShade 对近白像素乘 >1 系数未夹紧 → 24 位颜色打包溢出串通道 → 雪块边缘红黄黑杂色条带。改为经 shade() 夹紧；同时瓦片 UV 加半像素内缩防图集边界采样漂移。
6. **修复存档跨星球恢复**：世界生成改用存档所在行星的地形参数（此前读档恒用地球地形）。

### 验证
- `node tests/run.js`：**44239 通过，0 失败**（新增：太阳系 8 行星/真实行星名/土星环/表面类型多样化/地形参数接入生成/默认参数向后兼容）。
- `tools/verifysolar.mjs`：太阳系数据、星图面板真实行星名、火星 99% 红沙、海王星 99% 雪、地球 73% 草地、太空母星球体/太阳/土星环、截图视觉确认（火星橙红天红沙地、海王星雪地干净无杂色、太空俯瞰可见地球蓝绿球体）。
- 上两轮 `verifyround`/`verifyflight` 全量回归通过。

## 下一步（第 10 轮建议，按既定优先级）
1. **太阳系内更深度差异化**：专属资源/矿（如火星铁矿、冰星二氢晶体）、环境危险（金星高温/冰星低温对危险防护的影响）、天气/沙暴。
2. **空间站**（绕地球/太阳轨道）：交易/任务/造船中心，星图标记。
3. **NPC 与世界背景**（空间站对话/商店）。
4. **多星系与大飞船**：比邻星系（三体）任务线，大飞船购买/制造。

## 第 10 轮（已完成）：太阳系内星球深度差异化——环境危险 / 沙暴暴雪 / 专属资源

### 内容
1. **环境危险系统**（纯函数 `environmentDrain`，行星配置驱动）：
   - 金星=剧毒大气（全天消耗 2.0/s）、水星=烈日灼烧（白天 2.6/s）、火星=夜间严寒（夜晚 1.2/s）、木星/土星=辐射风暴（全天 1.8/1.5）、天王星/海王星=极寒低温（全天 2.2/2.4）、地球=夜间低温（与原行为一致）。
   - 危险防护条持续消耗，归零后扣血；HUD 右上角红色 ⚠ 环境徽章 + 落地警告 toast。
2. **沙暴/暴雪天气系统**（`entities/weather.js`）：
   - 金星/火星/水星/木星/土星=沙暴，天王星/海王星=暴雪；随机风暴循环（8~18s 判定、持续 25~45s）。
   - 效果：黄褐/白色斜纹屏幕滤镜、横扫沙粒/雪花粒子、风声增强、**危险消耗 ×2.5**、徽章切换"沙暴中/暴雪中"；驾驶飞船自动淡出。
3. **专属资源分布**（行星级倍率，接入 WorldGen 矿脉/植物生成）：
   - 火星=铁氧体富集 2.6×（实测已加载区块 28.4 万 vs 地球 8.1 万，3.5 倍）、铜 1.8×；金星=金矿 2.2×、钠花 2×；水星=煤 2.2×；冰巨星=二氢晶体 2.5~3×（雪原/红沙上现在也会生长晶体）、氧草 2~2.5×；木星/土星=碳晶 2×。
4. 跨行星跃迁/读档/重新生成均正确携带 hazard/weather/ores/plants 配置。

### 验证
- `node tests/run.js`：**44249 通过，0 失败**（新增：environmentDrain 全分支、火星铁/金星金/冰星二氢富集对比）。
- `tools/verifyweather.mjs`：金星全天消耗+徽章、火星夜耗昼回、沙暴强度/滤镜/2.36 倍危险、海王星暴雪、火星 vs 地球铁氧体密度对比，全部通过；截图视觉确认沙暴黄褐斜纹+沙粒、暴雪白色斜纹+雪花、⚠ 警示徽章。
- 上三轮 `verifyround`/`verifyflight`/`verifysolar` 全量回归通过。

## 下一步（第 11 轮）：空间站
- 绕地球/太阳轨道的大型空间站实体（太空模式可见、接近后可停靠按 E 交互）。
- 功能：任务中心 / 交易中心 / 飞船制造与购买点；星图标记空间站位置。

## 第 11 轮（已完成）：空间站（交易/任务/船坞 + 星图标记）

### 内容
1. **空间站实体**（`entities/station.js`）：体素风轨道站——核心舱 + 环形居住舱 + 4 组太阳能板 + 对接天线 + 青/琥珀信标灯 + 名称标签。位于**地球"静止轨道"**（进入太空点 + 固定偏移，独立于随玩家移动的行星球组——初版挂在行星组下导致永远无法接近，已修复）。自转缓慢，仅地球轨道出现；离站/跃迁自动隐藏或重建。
2. **停靠流程**：太空驾驶接近（<55m）→ 提示"按 E 停靠空间站"→ 停靠打开面板（游戏冻结）→ [Esc] 离站恢复飞行。星图新增"地球轨道空间站"条目，可设定为目标（罗盘指向，id=100）。
3. **交易中心**：出售资源换信用点（新货币 `credits`，含专属图标）+ 购买物资（金属镀层/密封胶/发射燃料/玻璃/外壳等），价格表 `systems/station.js`。
4. **任务中心**：3 个收购订单（铁氧体供应×15→60 点、贵金属回收×5→75 点、燃料补给×2→70 点），一次性奖励比散卖更划算。
5. **飞船船坞**：引擎升级 Lv1/Lv2（大气层最大速度 30→40→50）与护盾扩容 Lv1/Lv2（护盾上限 100→150→200），需逐级购买、永久生效、随存档保存。

### 验证
- `node tests/run.js`：**44275 通过，0 失败**（新增：空间站经济——出售/购买/订单交付/升级逐级限制/价格表注册/存档升级字段）。
- `tools/verifystation.mjs`：星图空间站行与设目标(100)、空间站世界坐标与罗盘、接近检测→E 停靠→面板(30 行)→Esc 离站、出售 20 铁氧体=60 点、订单+60、引擎/护盾升级后大气层最大速度 40/护盾上限 150，全部通过；截图视觉确认空间站外观（核心/环舱/太阳能板/信标/标签）、交易面板、星图 8 行星+空间站条目。
- 前四轮 verifyround/verifyflight/verifysolar/verifyweather 全量回归通过。

## 下一步（第 12 轮）：NPC 与世界观
- 空间站 NPC（站台管理官/商人/工程师等）对话面板：背景故事、站台介绍、交易引导。
- 地球坠机点新增残骸日志（世界观文本收集）。

## 第 12 轮（已完成）：NPC 与世界观（四块规划第 3 块）

### 内容
1. **空间站 NPC**（`systems/npcs.js`）：站台"人员"页新增 3 名 NPC——站长·卡伊拉（管理官）、商人·维罗（交易官）、工程师·泰莎（船坞技师）。点击进入对话面板，"下一句"循环对话、"返回"回列表。台词铺世界观并为下一块（比邻星/大飞船）埋线：四十年前"无人深空计划"殖民舰队失踪、深空天线收到比邻星方向脉冲、船坞封存大船蓝图。
2. **坠机点数据日志**（`entities/logs.js` + `systems/npcs.js`）：3 篇世界观文本（航行日志 #1 坠毁前 / #2 船长指令 / 求救信标）。表现：焦土区地面悬浮的青色全息立方（带光环、旋转漂浮动画）；靠近（<3.2m）提示"按 E 读取数据日志"，E 打开日志面板（已读/未读列表、点击重读、Esc 关闭）；已读变暗。收集状态随存档保存（`logs` 字段）。
3. 数据日志优先于飞船互动提示与交互（靠近日志按 E 先读日志）。

### 验证
- `node tests/run.js`：**44298 通过，0 失败**（新增 NPC/日志数据完整性、id 唯一、查询函数）。
- `tools/verifynpc.mjs`：停靠→人员页 3 NPC→对话循环/返回；走近日志→提示→E 读取→面板(3 项列表/正文)→Esc 关闭→未读跳过已读→存档含 logs，全部通过。
- 截图视觉确认：NPC 对话面板（台词+下一句/返回）、全息日志立方体悬浮于焦土地面（近距离青色发光方块+光环）。
- 前五轮 verifyround/verifyflight/verifysolar/verifyweather/verifystation 全量回归通过。

## 下一步（第 13 轮）：多星系大任务 + 大飞船（四块规划第 4 块）
- 比邻星（三体）任务线：站长任务接取 → 深空天线信号 → 修复/升级飞船 → 飞往比邻星（多星系跳转：太阳系 ↔ 比邻星系）。
- 大飞船：空间站船坞购买/制造（已铺蓝图伏笔），更大货舱/更高脉冲速度。

## 第 13 轮（已完成）：多星系大任务 + 大飞船（四块规划第 4 块，全部完成）

### 内容
1. **双星系架构**（`space/system.js`）：太阳系（8 行星）↔ 比邻星系（3 行星：比邻星 b 潮汐锁定荒原 / c 冰封世界 / d 环带气态巨行星）。`generateSystem(seed, defs)` 泛化生成器，两星系确定性生成；`SpaceSystem.galaxies/galaxyId` 双星系数据 + 各自 visited 集合。
2. **跨星系跃迁门**：当前星系固定方位的大型青色跃迁环（双环+内芯光晕+名称标签，自转），世界坐标=进入太空点+偏移。接近 <150m 自动跃迁（带跃迁闪屏与音效）；无大船时接近仅警告（"需大型殖民船"）。星图新增跃迁门条目：无大船显示"需大型飞船"锁定，目标/罗盘/穿越全链路。
3. **大飞船**：空间站船坞新增"大型殖民船 · 曙光号"（1500 信用点，需引擎 Lv2 + 护盾 Lv1 前置）：解锁跨星系跃迁、太空脉冲速度 420→540、船体放大 1.3×、永久生效随存档保存。
4. **任务链 13→17 步**：探索另一颗星球 → 造访地球轨道空间站（停靠即完成）→ 与站长了解深空信号（与卡伊拉对话）→ 购买大型殖民船 → 跃迁至比邻星系（大任务终点 → 旅程完成面板，遍历星系数=两星系之和）。
5. 存档兼容：新增 galaxyId/visitedProxima/bigship 字段，旧存档默认太阳系；读档按存档星系恢复世界地形/色板/天气。

### 验证
- `node tests/run.js`：**44313 通过，0 失败**（新增：任务链 17 步全流程、比邻星系生成、跃迁门锁定、大船前置/购买、存档星系字段）。
- `tools/verifyproxima.mjs`：跃迁门锁定(星图禁用+setTarget 拒绝)→任务推进(停靠→站长→购船→跃迁)→大船(引擎2/护盾1/缩放1.3/脉冲540)→门解锁+罗盘 4468m→穿越跃迁→比邻星系(3 行星/比邻星 b/barren 地表/任务完成)→比邻星图(返回门/无空间站行)，全部通过；截图视觉确认青色跃迁环+名称标签、比邻星 b 棕褐行星。
- 前六轮全部 verify* 浏览器套件回归通过。

### 四块规划全部完成 ✅
① 星球环境差异化（第 10 轮）→ ② 空间站（第 11 轮）→ ③ NPC 与世界观（第 12 轮）→ ④ 多星系+大飞船（本轮）。

## 第 14 轮（已完成）：菜单/面板进出体验修复（修理页重弹、设置被覆盖等同类 bug 举一反三）

### 根因审计（逐行确认）
1. **z-index 层叠倒挂**：`.screen`（主菜单/暂停，z=40）> `.fullpanel`（设置等，z=30）→ 点「设置」时面板被菜单压在下面看不见；退出菜单后设置才露出来（用户报告 2）。
2. **陈旧按键跨面板转换**：`pressedSet` 边沿事件存活 2 帧；面板打开期间按的 E 未被消费，Esc 关闭并重锁指针后 `player.update` 读到残留 E → `onInteract()` → 修理页立即重开（用户报告 1 的"又重新弹出"）。同类波及 Tab/M/停靠 E。
3. **指针锁与面板竞态**：`onGesture` 重锁前清 `wantLock`，失败后不再重试；重锁请求在 `openRepair→exitLock` 之后成功 → "面板开着但指针已锁" → 第一次 Esc 被 Chrome 吞掉（只退锁不关面板）＝"退出不了"。关闭面板后 rAF 内 `requestLock` 命中浏览器冷却被拒 → `pointerlockerror` → 误弹暂停、恢复时抖动。
4. **inMenu 单布尔与多面板**：旅程面板可叠在其他面板上；各 `close*` 盲目置 `inMenu=false`。
5. **Esc 链缺口**：旅程面板无 Esc 关闭；指针未锁时（驾驶/卡死态）按 Esc 无任何效果；主菜单点开始后残留按键会在出生点弹面板。

### 修复清单
1. `src/core/input.js`：新增 `clearTransients()`（清边沿事件+鼠标增量，保留 `down` 按住语义）；`resumeFromPause` 复用。
2. `src/core/ui.js`：新增 `journeyVisible()`。
3. `src/core/game.js`（核心重构，公开方法名不变）：
   - **面板状态机**：`refreshInMenu()` 从 7 个面板的真实 DOM 可见性推导 `inMenu`（不再各面板手写布尔）；`openPanel/closePanel` 统一退锁→清残留按键→显示/隐藏→推导 inMenu→按需重锁；全部 open/close/dock/undock/showJourney 走助手，新增 `closeJourney()`。
   - **锁竞态修复**：`onLockChange(true)` 时若 `inMenu` 立即退锁（禁止"面板开+指针锁"）；`onLockChange(false)` 时 `wantLock` 待锁则只重试不弹暂停（消除误弹/抖动）；`onGesture` 重锁不再提前清 `wantLock`，失败可反复重试。
   - **loop 链**：新增旅程面板 Esc 关闭；新增解锁态 Esc 兜底开暂停（驾驶/卡死态不再失灵）；链尾"面板打开期间吞咽 E/Tab/I/M"（陈旧按键直接作废）。
   - `startPlaying()` 清残留按键，出生点不再误弹面板；`exitToMenu/quitGame` 清 `wantLock`。
4. `style.css`：`.fullpanel` z-index 30→45（高于 `.screen` 40、低于 toasts 46）：设置从主菜单/暂停打开正确覆盖、可交互；Esc 逐层返回（设置→菜单/暂停→游戏）。

### 验证
- `node tests/run.js`：**44313 通过，0 失败**。
- 新增 `tools/verifypanels.mjs`（13 项断言全过，`ERRORS: none`）：设置盖暂停/盖主菜单（z-index+命中测试）→Esc 逐层返回；修理页"开→残留 E+Esc 关→不重弹"；按住 E 关闭不重开；重锁失败不误弹暂停且 wantLock 保持；背包 Tab/Tab/Esc；星图 M/Esc；空间站停靠/离站；日志 E/Esc；旅程 Esc。截图 `shots35/` 视觉复核：设置面板完整可读、底层菜单按设计虚化在背后。
- 7 个既有 verify 套件（round/flight/solar/weather/station/npc/proxima）回归：全部 `ERRORS: none`。

## 第 14 轮（补充）：Esc 退出面板的误弹暂停 / 浏览器全屏退出 / 光标滞留修复

### 问题
1. 用 Esc 关修理/日志等面板后，"有时候"暂停菜单被调出来——多为连按第二下 Esc 时命中两种窗口之一：重锁待处理期（未锁）按 Esc 走了"开暂停"兜底分支；重锁已成功（已锁）时 Esc 被浏览器拦截→`pointerlockchange`→暂停。
2. 页面能收到 Esc 的场合（面板打开/暂停/未锁状态）没有 `preventDefault`，Esc 的默认动作（退出浏览器全屏）被浏览器执行 → "按 Esc 关面板把浏览器全屏也退了"。
3. 重锁请求失败（浏览器 1.25s 冷却/激活窗口过期）后光标保持自由，只能等用户再按一次键。

### 修复
1. `src/core/input.js`：keydown 对 `Escape` 一并 `preventDefault()`（与 Tab/Space 同列）——页面收到的 Esc 不再泄漏给浏览器默认动作（全屏退出）。
2. `src/core/game.js`：
   - `requestGameLock()` 记录 `relockGrace` 时刻；loop 的 Esc 兜底分支在**重锁缓冲窗口（1.5s）内吞掉 Esc**（onGesture 已借本次手势重试锁），缓冲期后 Esc 仍正常打开暂停（卡死态仍可暂停）。
   - 重锁失败 → `scheduleRelockRetry()` 在 ~1.35s 后自动重试（上限 2 次，避开浏览器冷却），"光标变自由"状态自愈；`onLockChange(true)`/`exitToMenu`/`quitGame` 清理定时器。
   - 卡死提示阈值 0.9s→1.6s：给自动重试留静默期，避免每次关面板都刷提示。
3. `tools/verifypanels.mjs` 新增 4 项断言（合计 17 项全过）：关面板后第二下 Esc 不弹暂停（缓冲窗口）；缓冲期后 Esc 正常暂停；合成 Esc keydown `defaultPrevented=true`；重锁失败自动重试已排程。

### 验证
- `node tests/run.js`：**44313 通过，0 失败**；`tools/verifypanels.mjs` 17 项断言全过、`ERRORS: none`；7 个既有 verify 套件回归全部 `ERRORS: none`。

## 第 15 轮（已完成）：面板关闭键改为 B（Esc 专用于暂停菜单）

### 动机
Esc 身兼「关面板」与「暂停」两职，导致关面板时误弹暂停/退出浏览器全屏/光标滞留等问题（第 14 轮已修复副作用，但键位仍易误触）。用户要求把面板关闭键换成一个零冲突的键，已确认选择 **B**。

### 改动
1. `src/core/constants.js`：`KEY.CLOSE = 'KeyB'`（全局唯一，此前未占用；Esc 保留为暂停菜单键）。
2. `src/core/game.js`：loop 面板链每个分支的关闭条件追加 `|| closeKey`（B 键）并同步 consume——设置/背包/修理/空间站/日志/星图/旅程全部支持 B 键关闭；**Esc 保留为静默兜底**（不宣传但可用，第 14 轮机制已消除其副作用，3 个既有 verify 工具无需改动继续覆盖 Esc 路径）；B 在游戏进行中无任何副作用（不开面板/不暂停/不开火）。
3. `index.html`：全部提示文案改为 B——背包 `[B] 关闭 · [Tab] 也可`、维修 `[B] 关闭`、空间站 `[B] 离站`、日志 `[B] 关闭`、星图 `[B] 关闭 · [M] 也可`、设置 `[B] 返回 · [Esc] 也可`；HUD 快捷键行与主菜单操作表追加 `[B] 关面板 / 关闭面板`。
4. `tools/verifypanels.mjs`：新增 10 项断言（合计 27 项全过）——B 键逐个关闭 7 个面板（含设置盖暂停/盖主菜单两种场景）、B 在游戏进行中零副作用、提示文案含 `[B]`；顺带修复了空间站章节移动船体后修理页复位的测试前置。

### 验证
- `node tests/run.js`：**44313 通过，0 失败**。
- `tools/verifypanels.mjs`：27 项断言全过、`ERRORS: none`。
- 7 个既有 verify 套件回归：全部 `ERRORS: none`。

## 第 16 轮（已完成）：新玩家开场体验——反剧透任务面板 / 去调试痕迹 / 图标与提示打磨

长期目标从"加功能"转向"把 Demo 打磨成可发给陌生玩家试玩的小游戏"。本轮用无头浏览器以全新存档完整"体验"开场前 10 分钟（`tools/uxaudit.mjs` 截图 + 视觉模型逐屏审查），定位并修复最影响第一印象的问题。

### 修复清单
1. **任务面板反剧透 + 防遮挡**（`systems/quests.js` 新增纯函数 `missionView()` + `core/ui.js` 重写 `setMissions`）：出生第一分钟原来一次性列出全部 17 步任务（直接剧透终局"跃迁至比邻星系"，且左侧面板过长遮挡视野）。现在只显示：已完成计数、当前步骤（带进度）、接下来 2 步、其余折叠为"… 后续目标 ×N"；面板标题带进度"任务 // MISSIONS · x/17"（只给数字不给内容，保留成就感不剧透）。乱序提前完成的步骤不进预告列表。
2. **移除调试热键 G**（`constants.js`/`game.js`）：G 键原来瞬间重建世界（无确认），陌生玩家误按 = 进度全毁；主菜单"按 G 重新生成星球"提示一并删除（保留种子显示）。`regenerate()` 整体移除，`KEY.NEW_PLANET` 删除。
3. **菜单/HUD 文案去调试味**（`index.html`）："X 飞行模式(开发)" → "X 飞行模式"；HUD 常驻快捷键行去掉"[B] 关面板"（B 只在面板打开时有效，各面板自带提示；常驻显示会让玩家误以为随时可按）。
4. **日志→修船引导**（`game.js` `closeLogPanel`）：开场任务说"检查飞船"，但坠机点旁按 E 会先读到数据日志（日志优先设计）→ 视觉审查发现新玩家困惑。现在任务第一步读日志关闭后弹一句"数据已记录 · 继续任务：靠近飞船机身按 E 检查"；已完成该步/面板打开期间不弹冗余提示。
5. **合成图标对比度**（`world/textures.js`）：强化玻璃图标旧版主体 alpha=46 在深色底上几乎隐形 → 加宽不透明边框+主体 150+高光；碳晶体调亮（0x7a2f3f→0xb0506a 系）。视觉确认两种图标清晰可辨。
6. **toast 遮挡面板**（`style.css` + `game.js` `refreshInMenu`）："扫描完成"等 toast 默认 bottom:92px，正好叠在居中面板的合成列表上 → 面板打开时 body.panel-open 把 toast 移到 bottom:20px 屏幕最底部，关闭恢复。
7. **数据日志信标化**（`entities/logs.js`）：视觉审查发现日志点像"简陋白色十字占位物"。升级为科幻信标：垂直青色光柱（AdditiveBlending，远处即可定位）+ 全息内芯呼吸脉冲 + 光环；已读后光柱熄灭、立方体变暗、光环淡出（进度反馈清晰）。新增 `tools/logview.mjs` 视觉验证（7 项断言）。
8. **坠机场景重做——出生第一眼必须看到飞船**（视觉审查发现：新玩家出生第一眼完全看不到任务目标飞船，只能靠文字导航）：
   - **根因一（朝向）**：坠毁点选在"最低洼处"，可能在出生点任意方位，而出生视角固定朝 +x → 部分种子开局直接背对飞船。→ `game.js` 出生时按坠毁点方位计算 yaw 对准飞船。
   - **根因二（高度）**：弹坑中心下挖 ~3 格 ≈ 飞船全高，船体整个陷在坑里，被坑沿挡住。→ `world.js`：弹坑深度 3.2→2.2；新增 `buildWreckMound()` 在坑底堆 3 层残骸堆（金属板/焦土/石块，边缘打洞），飞船抬高 4.05 格落在堆上——机身/机翼/尾翼明显高出坑沿。
   - **根因三（遮挡）**：`carveCrash` 新增"出生点→坠毁点视线走廊"清树（线段 4.5 格宽 + 出生点周围 6 格）。
   - **氛围**：余烟加强（双烟源×3 粒子/0.3s、尺寸 0.5、寿命 4.2s，实测烟柱 50+ 粒子升 10 格）；船体侧倾 -0.13/-0.09 强化坠毁姿态。
   - 视觉评分：坠机场景第一印象 **7/10**（出生视角飞船+烟柱清晰可辨，倾斜残骸+焦土+残骸堆氛围成立）。新增 `tools/shipview.mjs` 三视角截图工具 + verifyfirstplay 3 项回归（出生朝向对准/飞船高出坑沿 2 格/视线走廊无树）。

### 验证
- `node tests/run.js`：**44326 通过，0 失败**（新增 missionView 渐进显示 13 项断言：开局视图/推进/乱序完成/终局）。
- 新增 `tools/verifyfirstplay.mjs`（31 项断言全过，`ERRORS: none`）：文案无调试痕迹、开局面板 4 行不剧透、完成 1 步后同步、G 键零副作用（种子/任务/位置不变）、日志指引正反向、玻璃/碳图标像素级对比度、toast 贴底与恢复、出生朝向对准飞船、飞船高出坑沿、视线走廊无树、全页无"开发/调试"字样。
- 新增 `tools/logview.mjs`（7 项）与 `tools/shipview.mjs`（三视角截图）视觉验证工具。
- 9 个既有 verify/flow/flight 套件全量回归通过。
- `tools/uxaudit.mjs` + 视觉模型复查：任务面板 4 行清爽、图标可辨、toast 不叠面板；坠机场景第一印象 7/10（出生即见飞船+烟柱）。

### 下一步（继续按"体验→修复"循环，候选）
- 开场叙事薄弱：坠机只有一段文字，无开场镜头/动画，第一印象冲击力不足。
- 内容密度：仅 7 配方、1 种敌对生物；新玩家 15 分钟后的探索正反馈偏弱。
- 音效有 UI/环境声但无背景音乐。
- 夜间生物 AI 单调（直线追击）；无成就/收集系统。
- 待长测：数小时存档膨胀、多次跨星系往返、低配机性能。

## 第 17 轮（已完成）：生存补给循环 + 夜间战斗深度（把已有系统做深）

继续"体验→修复"循环。本轮聚焦 5–15 分钟阶段的两个核心空洞：**采到的钠/氧没有任何用途**（死资源），以及**夜间战斗没有威胁且庇护所毫无意义**（怪物穿墙、隔墙照打）。

### 内容
1. **生存补给循环**（`player.js`）：钠/氧此前是"死资源"（钠花、氧草挖了就堆仓库）。现在：
   - 危险防护 < 15 → 自动消耗 1 钠 +50（危险星球/夜晚沙暴的续航手段）；
   - 生命维持 < 15 → 自动消耗 1 氧 +60（洞穴探索的续航手段）；
   - 首次拾取钠/氧弹用途提示（只提示一次）；危险防护 < 25 弹警告"采集钠花可自动补充"。
   - 创造模式免疫。探索采集→环境续航的正反馈闭环成立（钠花/氧草从"路过"变"必采"）。
2. **夜间战斗深度**（`mobs.js`）：
   - **怪物不再穿墙**：水平移动分轴碰撞（此前怪物直接穿过庇护所墙体，建造任务形同虚设）；
   - **隔墙/隔层不攻击**：攻击需要水平距离 <2.4 + 垂直差 <2.5 + 视线采样（1/3、1/2、2/3 点无实心方块）——躲进庇护所或站上高柱真正安全；
   - **威胁提升**：攻击 9→14、冷却 1.15s→1.0s、追击速度 2.1~3.6 → 3.1~4.3（略慢于步行 4.4，保持可逃性但夜晚有压迫感；6 只齐袭会致命）。

### 验证
- `node tests/run.js`：**44326 通过，0 失败**（本轮为浏览器侧行为，新增两套验证）。
- 新增 `tools/verifysurvival.mjs`（15 项全过）：钠/氧自动消耗、+50/+60 数值、阈值不误耗、toast 反馈、库存空不崩溃、创造免疫、首次提示只弹一次、低危险警告。
- 新增 `tools/verifycombat.mjs`（9 项全过）：隔墙停步不穿墙、隔墙不掉血、视线判定、开阔地近战掉血、4 格高处安全、白天燃烧、追击速度（测量逼近峰值，规避到达后的振荡）。
- 新增 `tools/soaktest.mjs`（15 项全过）：**50 分钟连续游玩稳定性模拟**（6000 次固定步快进 + 昼夜循环 + 移动/挖掘/扫描/开背包 + 夜间刷怪 6 只）——无页面错误、内存 24→91MB 有界、区块 323 保持卸载窗口内、脏队列收敛、粒子/能量弹无泄漏；保存→重新加载→点"继续游戏"后背包/任务进度/飞船升级/日志收集**逐项完整恢复**（对应"长时间连续游玩 + 保存/退出/重新载入"验收项）。
- 既有 16 套 verify/flow/flight 套件全量回归通过。

## 第 18 轮（已完成）：开场俯瞰镜头 + 背景音乐（第一印象冲击力）

### 内容
1. **开场镜头**（`game.js` `beginIntro/updateIntro/endIntro`）：新游戏点"开始"后，5.5 秒三段式俯瞰——高空全景（出生点上方 35m 看坠机点）→ 低空掠过残骸 → 滑入玩家眼睛，最后视角平滑转向出生朝向；黑色遮罩淡入开场。期间世界照常运转（天空/粒子/烟柱是活的），玩家物理与快捷键暂停（结束无跳帧累积），HUD 与枪模隐藏；**任意按键/点击可跳过**；结束弹"意识恢复 · 检查你的飞船残骸"并进入正常引导。继续存档时跳过（老玩家不被镜头拖沓）。
2. **背景音乐**（`audio.js` 程序化氛围乐，延续全原创合成路线）：持续低音垫（3 失谐振荡器 + 慢速 LFO 滤波开合）+ 稀疏琶音调度器（AudioContext 前瞻排音符，五声音阶随机上下行）。**菜单模式**（A1 低音/明亮音阶）与**游戏模式**（C2 低音/低沉音阶）切换：主菜单/退出到菜单 = 菜单乐，进入游戏 = 游戏乐。遵守浏览器自动播放策略——首帧只记录请求，用户手势后开播。

### 验证
- `node tests/run.js`：**44326 通过，0 失败**。
- 新增 `tools/verifyintro.mjs`（17 项全过）：新游戏触发镜头/HUD 隐藏/相机高空/遮罩/输入抑制/枪模隐藏、任意键跳过、自然播完、相机回位、继续存档不播、菜单→游戏→菜单音乐切换、手势后开播、节点建立。
- 视觉确认：俯瞰构图以坠机点为焦点、无穿模黑屏；结束自然回到第一人称 HUD 完整——"像成品沙盒游戏的开场"。
- 全量回归 18 套通过。

## 第 19 轮（已完成）：满背包资源丢失 Bug + 多次跨星系往返长测

### 修复（阻碍/破坏体验类 Bug）
审计发现"先扣材料、后加产物"的资源丢失漏洞，背包满时材料/信用点凭空消失：
1. **合成吞材料**：`craft()` 原先不检查产物能否放入——满背包合成扣掉铁氧体/二氢但产物消失。→ `Inventory.canAdd()` 容量预检 + `craft()` 先预检再扣料；背包满时合成按钮直接置灰（不再是点了才报错），误点提示"背包已满，无法合成"。
2. **空间站购买吞信用点**（`stationBuy`）、**出售吞货物**（`stationSell`）、**订单交付吞材料**（`stationDeliver`）：同型漏洞全部加容量预检；UI 区分"信用点不足 / 背包已满"提示。
3. 回归测试：单元测试新增 15 项（canAdd 边界/满背包合成/购买/出售/交付不吞不丢）。

### 长测补全（验收项："多次跨星系往返" + "极端背包状态"）
- 新增 `tools/verifyroundtrip.mjs`（12 项全过）：**太阳系 ↔ 比邻星系连续 4 次跃迁**（200→201→200→201），全程内存 26–34MB 稳定、行星数据/任务状态正确；**在比邻星系存档→读档**（恢复星系/升级/任务一致）、返回太阳系后再次存档读档正确。
- 新增 `tools/verifyinventory.mjs`（7 项全过）：满背包合成按钮置灰、合成/购买被拒不吞材料与信用点、提示文案、满背包挖掘不崩溃。

### 验证
- `node tests/run.js`：**44341 通过，0 失败**。
- 全量回归 20 套浏览器套件通过，`ERRORS: none`。

## 第 20 轮（已完成）：行星地表异常点——探索奖励循环（世界不再空洞）

### 内容（`entities/anomalies.js` 新系统）
此前星球地表只有矿和植物，步行探索没有"发现感"。新增确定性散布的**异常点**三类：
1. **古代石碑遗迹**：残柱+斜靠石板+发光核心 → 金矿×2+铜矿×3+信用点 45；
2. **坠毁无人机残骸**：机身+折翼+青色光点 → 金属镀层+钠×3+氧×2；
3. **应急补给箱**：箱体+绿色发光封条 → 碳×6+铁氧体×4+二氢×3。
- **确定性生成**：世界种子+星球 id 混入哈希，每星球 ~27 个散布在出生点 ±182 块（26 格网格采样）；离坠机点 24 块以上（不干扰开场叙事）；行星口味差异（偶数星球多无人机/奇数多补给箱）。
- **扫描集成**：C 键扫描把未收集异常标成**品红色菱形**（区别于资源青蓝/飞船琥珀），toast 追加"异常信号 ×N"——这就是"去哪里找"的答案。
- **调查交互**：靠近 3.2m 提示"按 E 调查 …"→ E 领取奖励（粒子+音效+弹窗）；领过即暗（核心变灰、不再提示、不可重复领取）；**满背包拒绝并提示**（复用第 19 轮容量预检）；收集状态按星球 id 隔离、随存档保存、读档恢复。
- 老存档兼容：无 anomalies 字段的旧档读档 → 空集（单元测试覆盖）。

### 验证
- `node tests/run.js`：**44344 通过，0 失败**（新增异常状态存档往返/老档兼容断言）。
- 新增 `tools/verifyanomaly.mjs`（15 项全过）：生成数量/类型齐全/远离坠机点/同种子布局确定、扫描品红标记与计数、靠近提示、E 领奖、防重复领取、满背包拒绝、存档读档恢复收集状态、截图视觉确认（调查完成 toast + 场景协调）。
- 全量回归 21 套通过，`ERRORS: none`。

## 第 21 轮（已完成）：里程碑（成就）系统——全程成长反馈脉冲

### 内容（`systems/milestones.js` 新系统）
游玩过程此前只有任务列表这一条反馈线。新增 **16 项里程碑**，与所有现有系统咬合（纯数据+纯逻辑，可测试）：
- 挖掘线：第一次挖掘 / 矿工（100 方块）；建造：小小建筑师（12）；合成：第一件造物 / 熟练工匠（10 次）
- 战斗：猎手 / 生物清除者（10 只）；探索：坠机档案（3 日志）/ 遗迹调查员（3 异常点）
- 经济：太空商人（赚 100 信用点）；飞船：机械师（完全修复）/ 起飞 / 宇航员 / 星际旅人（2 行星）/ 空间站访客 / 星系跳跃者
- **即时反馈**：达标弹"★ 里程碑达成：…"toast（quest 音效）；不重复触发；一次大数值可一次点亮多档
- **面板**：暂停菜单新增"里程碑"按钮 → 双列 16 卡片（★金色已达/🔒灰锁 + 描述 + x/16 计数），B/Esc 关闭回暂停
- 存档往返 + 老档兼容（无 milestones 字段 → 空进度）；挖掘计数含无掉落方块（修 bug：原放 drop 分支内，焦土等无掉落方块不计）

### 验证
- `node tests/run.js`：**44354 通过，0 失败**（里程碑触发/去重/存档/兼容 10 项）。
- 新增 `tools/verifymilestones.mjs`（8 项全过）：挖掘/合成/击杀即时 toast、面板计数与行状态、B 关闭回暂停、读档恢复。
- 视觉确认：成就面板"达到商业成品游戏 UI 水准"（双列对齐、金/灰状态分明、无截断）。
- 全量回归 22 套通过，`ERRORS: none`。

## 第 22 轮（已完成）：夜间生物三种类（行星差异化）+ 风暴战斗压力长测

### 内容
1. **生物种类差异化**（`mobs.js` 参数化为 `MOB_KINDS`）：
   - **夜行兽**（原有）：均衡 30 血/14 伤/3.1 速；
   - **群居异虫**：10 血/6 伤/4.6 速（快脆，**成队 2-3 只**出现）；
   - **岩甲爬兽**：60 血/18 伤/2.2 速（慢厚重击，掉铜矿石）。
   - **行星决定构成**（世界丰富度）：荒芜/荒漠（火星/水星/比邻 b）→ 异虫+爬兽；繁茂（地球/金星）→ 夜行兽+异虫；冰/气态巨星 → 爬兽+夜行兽。夜间生物上限 6→8（虫群队形需要）。
2. **风暴+战斗+粒子+区块 压力长测**（新增 `tools/verifystormsoak.mjs`，10 项全过，对应验收项"战斗、天气、粒子和区块加载同时发生"）：恒定沙暴（强度≈1）+ 深夜 8 只怪物围攻 + 玩家绕圈移动/开火/挖掘/扫描，快进 15 分钟——无页面错误、粒子池稳在 640 环形上限、区块 323 有界、内存 104MB、能量弹零泄漏、风暴与战斗系统共存到结束（玩家 3 次"紧急生命维持"复活 = 生存张力真实存在）。截图确认沙暴暗黄浑浊氛围 + 徽章与危险提示。

### 验证
- `node tests/run.js`：**44354 通过，0 失败**（生物逻辑浏览器侧覆盖）。
- `tools/verifycombat.mjs` 扩至 **15 项全过**（新增：三血量/伤害/速度排序差异、爬兽掉铜矿、荒芜行星刷怪构成、虫群成队）。
- 全量回归 23 套通过，`ERRORS: none`。

## 第 23 轮（已完成）：新存档完整通关审计 + README 试玩说明重写（"可以发给别人玩"闭环）

### 内容
1. **README.md 全面重写**（旧版严重过时：G 键已删除仍列出、F/X 键位错误、内容停在第 5 轮、路线图里列着已实现的天气/成就）：现在包含——快速开始、玩法一览（坠毁求生→修船→太空→空间站→比邻星）、完整按键表（含 B/V/X/手电筒）、系统一览（行星/环境危险/天气/异常点/里程碑/经济/存档）、测试与项目结构、已知说明。新玩家拿到文件夹即可上手。
2. **`tools/verifyfullrun.mjs`——新存档完整通关审计**（16 项全过，对应验收项"新存档完整通关"的最终形态）：
   - 从主菜单点"开始"→ 开场镜头按键跳过 → 真实动作逐环走完 17 步：E 检查飞船 → 真实挖掘 15 方块 → 合成工具 → 清障放置 12 方块 → 二氢/铁氧体 → 镀层 → 修复三件（真实材料扣减）→ 登船 W+空格起飞 → 入太空 → **三次行星跃迁+降落**（第 1 行星完成探索任务 → 第 2 行星达成"星际旅人" → 回地球）→ E 停靠空间站 → 站长对话 → 购引擎/护盾/大船 → 跃迁门穿越比邻星系 → **旅程完成面板 17/17**。
   - 全程零页面错误；10 项里程碑伴随触发（第一次挖掘/建筑师/第一件造物/机械师/起飞/宇航员/星际旅人/空间站访客/星系跳跃者/熟练工匠）；截图视觉确认通关画面"接近成品游戏"。
   - 开发过程中顺带确认：起飞进太空需同时驱动 flight/space 双更新、放置需选中快捷栏、日志优先交互是设计行为——均属工具驱动方式问题，游戏本体无 bug。

### 验证
- `node tests/run.js`：**44354 通过，0 失败**。
- 新增 `tools/verifyfullrun.mjs`（16 项全过）。
- 全量回归 24 套通过，`ERRORS: none`。

## 第 24 轮（已完成）：护盾电池补给线 + 低配性能档案

### 内容
1. **护盾电池**（合成线从"任务专用"走向"生存装备"）：新配方 钠×3+铁氧体×2 → 护盾电池（新像素瓦片：蓝色能量罐+发光窗+电极）；护盾 <30% 时自动消耗 +60（与钠/氧自动补给同一闭环）；首次合成弹用途提示。战斗续航有了可制造的答案，钠/铁氧体在通关后仍有消耗出口。
2. **低配性能档案**（`tools/perflow.mjs`，9 项全过，对应验收项"性能在普通电脑上达到合理水平"）：低画质（低/渲染距离 4）与高画质（高/渲染距离 10）两档下持续行走+每 1.5s 传送 40 格（区块加载风暴）5 秒采样：
   - 低画质：**平均 215 FPS、p95 帧 6ms、最差 85ms、内存 49MB、区块 189**；
   - 高画质：平均 155 FPS、最差 135ms、内存 142MB。
   - 无 >1s 卡死帧、无页面错误——CPU 侧开销远低于可玩门槛，低画质档为核显/低配机留足余量。

### 验证
- `node tests/run.js`：**44365 通过，0 失败**（护盾电池配方/合成/扣减/满背包拒绝 +21 项）。
- `tools/verifysurvival.mjs` 扩至 **20 项全过**（护盾自动补给/阈值/toast/首次合成提示）。
- 全量回归 25 套通过，`ERRORS: none`。

## 第 25 轮（已完成）：中期体验审计——交易中心幽灵条目等完整感修复

### 体验审计（`tools/uxaudit2.mjs` 新增）
用无头浏览器把"陌生人玩到中期会看到的全部界面"逐屏截图 + 视觉模型审查：空间站四页签、9 配方合成表、星图、暂停菜单、设置面板。视觉结论"整体已接近成品"，但抓到一处**明显半成品痕迹**：

### 修复
1. **交易中心幽灵条目 `coal_carbon`**：`STATION_SELL` 价格表里残留一个未注册物品 id（碳的旧键），交易 UI 直接裸显英文变量名——中期玩家必见。→ 删除该条目（碳已有正式条目）。
2. **举一反三（防同类回归）**：单元测试新增价格表/订单注册完整性断言——出售表、购买表、收购订单的所有键必须是已注册物品（+32 项），此类"UI 裸显 ID"从此无法通过测试。

### 验证
- `node tests/run.js`：**44397 通过，0 失败**。
- `tools/uxaudit2.mjs` 复审：交易中心全部中文名称、无变量名残留；视觉模型确认"像成品界面"。
- 全量回归 25 套通过，`ERRORS: none`。

## 第 26 轮（已完成）：行星地标植被（地表剪影差异化）+ 死亡边界审计

### 内容
1. **行星地标植被**（`chunk.js` 新增第 6 阶段生成，解决"无树行星只有平地+低矮植物、地面剪影雷同"）：
   - **冰巨星**（海王星/天王星/比邻 c）：**二氢晶体塔**（2×1 双柱 2-4 格高 + 品红碳晶顶）——呼应"二氢富集"设定，可整塔采集；
   - **荒漠/荒原**（火星/水星/比邻 b）：岩石柱 2-3 格 + 35% 铁氧体帽；
   - **气态巨星**（木星/土星/比邻 d）：碳晶簇 2×1 双柱（呼应"碳×2"设定）；
   - 繁茂地球保持只有树。开发中视觉审查实测"单格十字晶体在蓝雪背景下几乎不可见"→ 加宽为 2×1 并加品红碳晶顶提高辨识度；像素级验证塔体渲染为 25% 密度的青色簇（视觉模型对蓝对蓝场景分辨能力有限，以像素数据为准）。
2. **死亡边界审计**（新增 `tools/deathprobe.mjs`，6 项全过）：步行/大气层驾驶/太空驾驶/空间站停靠/面板打开五种状态下被击杀——全部正确复活（满状态、无 NaN、模式一致、可继续游玩），闭合"非正常操作顺序和边界情况"验收项。

### 验证
- `node tests/run.js`：**44397 通过，0 失败**。
- 新增 `tools/verifysurface.mjs`（6 项全过：冰晶塔 85+/岩柱 67+铁帽/碳簇 120+/地球 0）+ `tools/deathprobe.mjs`（6 项全过）。
- 全量回归 27 套通过，`ERRORS: none`。

## 第 27 轮（已完成）：音量设置（音乐/音效分轨）+ 全成就达成时刻

### 内容
1. **音量控制**（此前游戏有音乐和全套音效但没有任何音量设置——发出去的必备项）：
   - 设置面板新增"音乐音量 / 音效音量"两个滑块（0–100%，即改即存 localStorage）；
   - 音频引擎分轨：音乐走独立 `musicBus`（音垫/琶音/音符全部接入），音效/环境/引擎走 master；`setMusicVolume/setSfxVolume` 即时生效；AudioContext 未建立时先记数值、首手势建立时取用；
   - 旧设置存档兼容（无音量字段 → 默认 60%/80%）。
2. **全成就达成时刻**：16 项里程碑全部达成 → 专属提示"★ 全成就达成 · 星际大师"（quest+warp 双音效）——通关后的自由探索阶段有了最终收集目标与闭环。

### 验证
- `node tests/run.js`：**44402 通过，0 失败**（音量规范化/默认/非法回落/旧配置兼容/全成就提示 5 项）。
- `tools/verifyfirstplay.mjs` 扩至 **36 项全过**（新增：滑块存在、修改生效、音频总线增益同步、标签更新）。
- 全量回归 27 套通过，`ERRORS: none`。

## 第 28 轮（已完成）：NPC 状态感知对话（台词随进度更新）

### 内容（`systems/npcs.js` 新增 `getNpcLines`，纯数据+纯逻辑）
此前 NPC 台词永远停在开局——买完大船回站，泰莎还在说"蓝图封存中"，对话明显过期。现在台词随游戏进度追加：
- **站长·卡伊拉**：买大船 → "曙光号已经出坞。带上这份星图——别让他们的信号成为绝响。"；到过比邻星 → "你回来了——比邻星的回音，比我们四十年的等待更珍贵。"（双条件时以比邻星台词优先）
- **商人·维罗**：买大船 → 提出跨星系运货生意；到过比邻星 → "新市场！这批货我全要了"
- **工程师·泰莎**：引擎升满 → 暗示去船坞深处；买大船 → "曙光号交给你了"（替换过期的"蓝图封存"）

空间站人员页按当前存档状态（bigship/visitedProxima/engine）解析台词后渲染，对话循环照常。

### 验证
- `node tests/run.js`：**44409 通过，0 失败**（7 项：无进度不追加/各条件台词/双条件优先级）。
- `tools/verifynpc.mjs` 新增状态台词浏览器断言（买大船后泰莎与站长台词实际出现在对话循环中），`CHECKS: all passed`。
- 全量回归 27 套通过，`ERRORS: none`。

## 第 29 轮（已完成）：背包整理 + 暂停菜单旅程概览（QoL 收尾）

### 内容
1. **整理背包按钮**（背包面板标题栏新增）：一键把 27 格背包区按物品中文名排序、空位沉底；**不动 9 格快捷栏**（避免选中项漂移），选中格保持；纯逻辑 `Inventory.sortPack()` 可测试。
2. **暂停菜单旅程概览**（暂停界面新增一行统计）：`探索行星 N 颗 · 信用点 X · 里程碑 m/16 · 任务 k/17`——每次暂停都回答"我玩到哪了"，与里程碑面板、任务面板构成完整的进度反馈体系。

### 验证
- `node tests/run.js`：**44414 通过，0 失败**（sortPack：不动快捷栏/按名排序/空位沉底/数量不变 4 项）。
- `tools/verifyfirstplay.mjs` 扩至 **38 项全过**（整理按钮行为 + 暂停概览齐全）。
- 全量回归 27 套通过，`ERRORS: none`。

## 第 30 轮（已完成）：干净环境交付验证 + 验收报告（"可以发给别人玩"最终证明）

### 内容
1. **干净环境验证**：`git clone` 到全新临时目录 → `npm install`（12 秒，仅 2 个包）→ `node server.js` → 单元测试 44414 全过 → verifyfullrun（完整通关 16 项）/verifyintro（17 项）在干净服务器上全部通过。确认 85 个文件全部被 git 追踪、无缺失依赖、无未追踪残留——"干净、易运行的试玩版本"验收项正式闭环。
2. **`FINAL-REPORT.md` 验收报告**：14 条验收标准逐项对照证据（每条指向具体测试/轮次），含干净环境验证记录、交付物清单与已知限制——供交付前审查。

### 验证
- 干净克隆：npm install → 服务器 → 单元 + 浏览器套件全部通过。
- 工作区全量回归 27 套通过，`ERRORS: none`。

## 第 31 轮（已完成）：存档导出/导入（备份与分享，试玩闭环最后一块）

### 内容
- 主菜单新增「导出存档」（下载 `voxelspace-save-<seed>.json`）与「导入存档」（选择文件 → 校验 → 写入 → 自动重载）。
- `save.js` 新增纯逻辑：`validateSaveData`（version=1 + 数字种子 + 大小上限）/ `exportSaveText` / `importSaveText`（非法 JSON/非法结构/空文本均拒绝且不动已有存档）。
- 玩家可备份存档、换浏览器/设备迁移、报 bug 时随存档一并发送——"发给别人玩"的反馈闭环完整。

### 验证
- `node tests/run.js`：**44421 通过，0 失败**（校验/导入拒绝路径 7 项）。
- 新增 `tools/verifysavetransfer.mjs`（8 项全过）：导出文本含种子与背包 → 清档 → 导入恢复；非法文本/结构被拒且存档不被改动；菜单按钮可见性；导入后读档内容完整恢复（背包/任务）。
- 全量回归 28 套通过，`ERRORS: none`。

## 第 32 轮（已完成）：宇宙地图第一阶段——统一天体数据模型 + 三级星图 + 真实轨道导航框架

### 目标背景
长期目标是把“若干可前往星球”升级为具有真实空间结构、明确层级与探索价值的宇宙。本轮按“先建立正确的空间模型、数据结构、地图层级和导航框架”的原则落地基础设施，内容丰富（卫星登陆/小行星带玩法/太阳视觉）留待后续轮次。

### 内容
1. **统一宇宙数据模型** `src/space/celestial.js`（纯数据 + 纯逻辑，Node 可测）：
   - 恒星 / 行星 / 卫星 / 小行星带 / 空间站 / 跃迁门使用同一套记录结构，未来新增轨道设施只需追加数据。
   - 太阳系 8 行星补齐真实天文数据：轨道半长轴（AU）、J2000 近似平黄经、半径、表面重力、自转周期、均温与事实卡；比邻星系 3 行星同样处理。
   - 主要卫星：月球、火卫一/二、四大伽利略卫星、土星 6 颗主要卫星、天王星 5 颗、海卫一——挂在统一 parentId 下，形成行星系统层级。
   - 尺度分离：`1 AU = 1000` 旅程单位（行星间真实比例：木星 5.20 / 海王星 30.11）；真实公里数保留在数据层用于信息面板；`visualRadius` 按真实半径立方根压缩；卫星轨道按母星球体放大（地图视觉尺度）。
   - 对数投影 `logProject`：内行星不挤团、外行星不出界，保证星图可读。
2. **太空运行时接入真实轨道**：`generateSystem` 改用 `aAU × AU` 与真实平黄经（仅 ±3° 种子扰动）；太阳从“跟随玩家的装饰球”改为**固定世界坐标节点**（相对地球 1 AU，可导航、有标签）；新增主小行星带固定节点（26 颗体素小天体簇）。
3. **三级星图 UI**（M 键）：
   - 恒星级：太阳系 / 比邻星系两个恒星节点 + 4.2465 光年连线 + 银河带背景；
   - 恒星系统图：中心恒星 + 全部行星（真实轨道顺序）+ 小行星带 + 空间站 + 跃迁门，对数轨道环带 AU 标签；
   - 行星系统图：母行星 + 卫星 + 轨道设施（如木星四大伽利略卫星）；
   - 画布点击选择天体 → 右侧真实信息面板（重力/温度/自转/事实）→“设定目标 / 进入行星系统 / 上级星图”；旧列表作为兼容层且内容跟随层级。
4. **统一导航闭环**：目标解析兼容旧数字 id（100 空间站 / 200/201 跃迁门 / 0-7 行星）与新字符串天体 id；`targetWorldOffset/compass/targetInfo` 统一走同一世界坐标解析；HUD 罗盘新增目标名称；右上角新增“星域 太阳系 · 地球轨道空间”定位；不可降落目标（太阳/小行星带/卫星）抵达后给一次性提示，不误导玩家。

### 验证
- `node tests/run.js`：**44467 通过，0 失败**（新增 46 项：真实轨道顺序与比例、统一注册表层级、地球/木星/土星卫星系统、恒星图数据、旅程尺度、对数投影、视觉半径、距离格式化、卫星轨道互不重叠）。
- 新增 `tools/verifyuniverse.mjs`：**20 项全过**（真实轨道比例、三级星图节点、画布可读、木星系统、太阳导航罗盘标签+1 AU 固定节点、旧数字 id 兼容、小行星带、比邻星系图），`ERRORS: none`。
- 回归：`verifypanels`（全过）、`verifysolar`（太阳真实节点语义更新后通过）、`verifyproxima`、`verifystation`、`verifyfullrun`（17 步通关全过）均 `ERRORS: none`。
- 视觉模型审查三轮截图：系统图清晰无重叠；发现并修复“恒星图/行星系统图下方列表仍显示太阳系行星”的层级错位；复审确认恒星级只有两个恒星节点、木星系统只显示木星与四卫星。

### 下一轮候选（按目标排序）
- 月球成为第一个可登陆卫星（月球地表/低重力/无大气，需世界与存档层级扩展）；
- 太阳真实视觉冲击（日冕着色/光芒/接近过热机制）与行星真实外观纹理；
- 行星本质差异化：重力、昼夜长度、太阳视大小接入地表玩法；
- 星图缩放/动态比例与轨道设施扩展。

## 第 33 轮（已完成）：行星本质差异化——真实重力/昼夜/太阳视大小/大气密度接入 + 行星与太阳视觉升级

### 目标
让“到了另一个世界”不再只靠颜色和资源倍率，而是关闭 UI 也能从物理手感和天空视觉上感知。本轮把第 32 轮统一数据模型里的真实天文参数接入玩法，并升级太阳与行星贴图。

### 内容
1. **行星物理差异化**（`celestial.js` 纯函数 → `game.applyBodyProfile` → `player.js`）：
   - 重力：`gravityFactor = 真实g / 9.81`（夹 0.15–2.0）：火星 0.378g（跳跃更飘）、木星夹到 2g（沉重）、月球数据已就绪 0.165g；
   - 坠落伤害阈值按动能换算：`-sqrt(2*g*6.2m)`，低重力世界阈值随重力下降；
   - 昼夜：`dayLengthSeconds` 保留真实自转快慢排序，平方根压缩到 120–1200 秒——木星 309s（快）、水星/金星 1200s；金星/天王星**逆行自转**（太阳西升东落）；
   - 太阳视大小：`1/sqrt(轨道半径)`——水星 1.61 倍、火星 0.81、木星 0.44、海王星 0.18；天空着色器 sunSharp/sunSoft 同步调整；
   - 大气密度：金星 2.3（浓雾压缩视野）、火星 0.55/水星 0.25（视野通透、星空更亮）；
   - 恒星颜色：比邻星系红矮星橙红光照（`starColorOf`）。
2. **行星太空贴图重做**（`system.js planetTexture`，768×384 程序纹理）：
   - 地球：低频大陆轮廓 + 高频海岸扰动，蓝海/绿地/褐岸/极冠，一眼可辨；
   - 火星：红壤 + 深色高地斑块 + 白色极冠；木星：横向湍流云带 + **大红斑**；土星：柔和带（配环）；冰巨星：青蓝渐变细带；荒岩：陨坑暗斑；金星：黄绿毒云旋涡。
3. **太阳视觉冲击**：白热光球 + 动态日冕 Shader（fresnel 脉动）+ 水平/垂直十字光芒；新增**真实方向太阳光** `spaceSunLight`（从太阳节点照向飞船/行星，明暗面与太阳位置一致）；`sky.setSpaceMode` 显式把场景背景切黑，退出太空恢复，杜绝太空露出地表蓝。
4. **大气雾距修正**：薄大气（火星/荒岩）视野 1.22×，浓大气（金星）0.87×——与危险/天气形成一致的世界感。

### 验证
- `node tests/run.js`：**44480 通过，0 失败**（新增 13 项：重力系数/月球低重力/木星夹值、昼夜压缩、太阳视大小递减、地球圆盘默认参数、大气密度、恒星光色）。
- 新增 `tools/verifyplanets.mjs`：**16 项全过**（地球基准档案、火星低重力+小太阳+薄大气、木星 2g+快昼夜、金星逆行+浓雾、贴图像素探针：地球海洋/大陆、火星红壤、木星大红斑、土星云带；太阳日冕与真实方向光；太空背景角像素全黑），`ERRORS: none`。
- 回归：`verifyfullrun`（17 步通关全过）、`verifyproxima`、`verifysolar`、`verifypanels`、`verifystation`、`verifyuniverse` 全部通过；`perf.mjs` 初始化 230ms、241 FPS，贴图分辨率提升无性能回退。
- 视觉模型审查（fast 初审 + precise 复核）：太阳黑色太空背景下亮核/日冕/十字光芒通过；地球球体平滑、海洋 70%+、绿色大陆、白色极冠、边缘大气辉光，无伪影。

### 下一轮候选
- 月球成为第一个可登陆卫星（低重力/无大气已具备参数基础，补世界与存档层级）；
- 木星/土星卫星的可停靠空间节点与小行星带开采玩法；
- 地表温度/风/重力对玩家动作的进一步反馈（低重力移动惯性、高重力冲刺消耗）。

## 第 34 轮（已完成）：月球成为首个可登陆卫星——完整登月闭环 + “月面漫步”里程碑

### 目标
把三级星图中的“行星系统”层级从纯地图升级为真正可探索的空间：地球的地月系统现在可以导航、跃迁、降落、出舱、采矿、起飞、返回，并且存档/读档完整恢复月面世界。

### 内容
1. **统一模型扩展**（`celestial.js`）：
   - 月球升级为 `landable:true`，补齐完整世界配置：`surface:'moon'`、月面地形/资源/月夜严寒危险、灰色天空色板、`sunAU:1`（月面太阳视大小=地球视角，而非按 0.0026 AU 放大）、`atmoDensity:0.2`（无大气）；
   - `moonSeedOf(gameSeed, navId)`：卫星世界种子确定性派生。
2. **月面世界生成**（`chunk.js`）：
   - `surfaceBlock('moon')` 全岩石地表；月面高度场叠加低频洼陷形成**环形山剪影**；
   - 月球贴图分支：灰白月尘 + 暗色月海 + 密集环形山斑。
3. **太空中的月球**（`system.js`）：
   - 母行星的可登陆卫星成为固定世界节点（灰色球体 + 贴图 + 标签），进入地球太空即可见，飞近自动跃迁；
   - `SpaceSystem.bodyId/visitedMoons`：当前天体可以是卫星；月轨上母行星（地球）作为最近邻反向出现，飞近可自动跃迁返回；
   - 空间站在月轨上不再误导出现；导航罗盘/目标偏移支持月球与返回地球。
4. **月面体验**（`game/player/sky`）：
   - 重力 = 地球 1/6（4.29 vs 26），跳跃飘浮、坠落阈值按动能换算；
   - 无大气：**黑天 + 白昼星空**（`sky.airless`：恒星天光、昼间星空、近零雾）、1200 秒慢昼夜、月夜严寒危险；
   - 月面没有坠毁遗迹（`noCrashSite`），保持原始月貌；
   - 修复了跨世界切换后数据日志/异常点未随新世界重建的隐性 bug（月球与每个新行星都会重建自己的探索实体）。
5. **成长反馈**：新增第 17 项里程碑 **“月面漫步”**（首次降落月球），里程碑面板/暂停概览自动同步。
6. **存档兼容**：存档新增 `bodyId/visitedMoons` 字段；旧存档无字段→行星表面；读档自动重建月面世界并恢复里程碑/访问记录。

### 验证
- `node tests/run.js`：**44486 通过，0 失败**（新增 6 项：月球可登陆配置/种子派生、月面全岩石地表/环形山起伏、旧存档 bodyId 兼容）。
- 新增 `tools/verifymoon.mjs`：**15 项全过**——地月系统图设目标 → 月球实体与罗盘 → 自动跃迁 → 月面世界（surface=moon/重力 4.29/无大气/黑天星空）→ 白昼星空与低重力下落实测 → “月面漫步”里程碑 → 存档重载后月面世界与进度完整恢复，`ERRORS: none`。
- 回归：`verifyfullrun`（17 步通关）、`verifymilestones`（17 项面板）、`verifyuniverse`、`verifyplanets`、`verifysolar`、`verifyproxima`、`verifypanels`、`verifystation` 全部通过；`perf.mjs` 初始化 213ms / 241 FPS。
- 视觉验证：从渲染缓冲直接导出 PNG（绕过合成器画布清空问题）——月球球体灰色 + 暗色月海纹理 + 黑底星空；月面灰白方块地表 + 黑色天空 + 昼间星点（体素风格统一保留）。期间修复“退出太空时无大气天体恢复地表蓝背景”的渲染 bug。

### 下一轮候选
- 木星/土星主要卫星逐个开放（数据已统一，复制月球路径即可）；小行星带资源开采节点；
- 月面专属探索内容（环形山异常点、月尘资源）；
- 行星地表导航层（罗盘地标/地图回放）。

## 第 35 轮（已完成）：太阳系 7 颗主要卫星全部可登陆 + 导航 HUD 距离/ETA + “卫星巡游者”里程碑

### 目标
月球路径验证成功后，把“卫星可登陆”从单点功能推广为太阳系的基础探索设施：木星、土星、海王星的主要卫星成为真实可探索世界；同时把导航 HUD 从“方向+原始单位”升级为符合宇宙尺度的距离与时间信息。

### 内容
1. **卫星统一开放**（`celestial.js` 数据驱动，无新增硬编码逻辑）：
   - 新开放：**木卫一（硫火山/高温）· 木卫二（冰下海洋/冰面）· 木卫三（巨卫星/荒岩）· 木卫四（古老冰岩）· 土卫六（浓密甲烷大气 atmoDensity 1.6）· 海卫一（逆行自转/深空极寒）**；
   - 每颗卫星独立：世界地形/资源/危险/天气/天空色板/太阳视大小（按母行星轨道）/真实重力；共 **7 颗可登陆卫星**；
   - 木星太空现在出现四大伽利略卫星实体，可导航、跃迁、降落、返回木星；土星/海王星路径同步生效。
2. **导航 HUD 升级**（目标距离与时间）：
   - 罗盘距离改为真实尺度文本：行星间显示 **AU**（如 0.34 AU），卫星/空间站显示公里；新增**预计抵达时间**（按当前飞船最大脉冲速度），让玩家明确“还要飞多久”；
   - 星图重定位验证：木星系统图中当前目标卫星高亮（琥珀虚线环）。
3. **里程碑扩展为 18 项**：新增 **“卫星巡游者”（降落 3 颗天然卫星）**；`moonwalker` 改为“降落天然卫星”通用描述。
4. **测试基础设施**：新增 `tools/verifyjovian.mjs`，验证木星跃迁 → 四卫星实体 → 欧罗巴目标/罗盘/ETA → 星图高亮 → 木卫二冰面世界（0.15g 下限/无大气/区域标签）→ 连续降落木卫一/木卫三 → 里程碑解锁。

### 验证
- `node tests/run.js`：**44489 通过，0 失败**（新增 3 项：伽利略卫星可登陆配置、土卫六浓密大气/海卫一逆行、≥7 颗可登陆卫星；里程碑总数 18）。
- `tools/verifyjovian.mjs`：**12 项全过**，`ERRORS: none`。
- 回归：`verifyfullrun`（17 步通关）、`verifymilestones`（18 项面板）、`verifymoon`、`verifyuniverse`、`verifyplanets`、`verifysolar`、`verifyproxima`、`verifypanels`、`verifystation` 全部通过。

### 下一轮候选
- 小行星带资源开采节点（金属/矿物采集与危险环境）；
- 天王星五卫星开放 + 行星地表导航层（罗盘地标/地表地图）；
- 恒星图扩展第三恒星系统。

## 第 36 轮（已完成）：主小行星带成为可开采太空节点 + 天王星五卫星开放（12 颗可登陆卫星）

### 目标
太空不能再只是“路过的空洞”：小行星带从视觉标记升级为具有实际探索价值与风险节奏的资源节点；天王星系统从地图层级升级为可登陆的完整卫星群。

### 内容
1. **主小行星带开采节点**：
   - 接近检测（`BELT_HARVEST_DIST=170`）：进入小行星带区域出现“按 E 开采”提示；
   - 开采循环：铁氧体粉尘/碳/铜矿石/金矿石按权重掉落，粒子喷发 + 物品弹窗；**3.5 秒冷却**与**每次进入太空 14 个矿点**构成节奏，采空需重新进入太空刷新；
   - 满背包不吞矿石（canAdd 预检）；E 键在驾驶中新增小行星开采分支；
   - 新增里程碑 **“小行星矿工”（开采 5 次）**，共 **19 项里程碑**。
2. **天王星五卫星全部可登陆**：天卫一（年轻冰谷）/天卫二（暗色冰卫星）/天卫三（冰岩）/天卫四（古老冰）/天卫五（破碎冰/维罗纳断崖）；各自独立世界配置（地形/资源/极寒危险/黑天星空/0.15g 可玩重力下限）。
   - 至此太阳系**可登陆天然卫星达到 12 颗**（月球 + 木星 4 + 土卫六 + 海卫一 + 天王星 5）。
3. **测试**：新增 `tools/verifybelt.mjs`（7 项）：矿点初始化 → 接近检测 → 开采收益/冷却 → 矿点耗尽与里程碑 → 天王星五卫星实体 → 天卫一冰面登陆。

### 验证
- `node tests/run.js`：**44492 通过，0 失败**（新增 3 项：天王星五卫星可登陆、12 颗可登陆卫星总数、小行星矿工里程碑）。
- `tools/verifybelt.mjs`：**7 项全过**，`ERRORS: none`。
- 回归：`verifyfullrun`（17 步通关，连续两次全过）、`verifymilestones`（19 项面板）、`verifyjovian`、`verifymoon`、`verifyuniverse`、`verifyplanets`、`verifysolar`、`verifyproxima`、`verifypanels`、`verifystation` 全部通过。

### 下一轮候选
- 行星地表导航层：地表罗盘地标（飞船/异常点/日志）与地图回放，补齐“恒星级→太阳系→行星系统→地表”最后一级；
- 小行星带更多玩法：稀有矿点/危险区/空间站订单联动；
- 恒星图扩展第三恒星系统。

## 第 37 轮（已完成）：行星地表导航层——四级星图最后一级（航点/地图/步行罗盘）

### 目标
补齐目标中的星图层级：**恒星级 → 太阳系地图 → 行星系统地图 → 行星地表导航**。玩家在地面按 M 不再“无事发生”，而是打开当前天体的地表航点图，选择目标后获得步行导航。

### 内容
1. **地表航点数据**（`Game.surfaceWaypoints()`，统一数据视图）：
   - 飞船（残骸/已修复状态）、3 篇数据日志（含已读状态）、全部异常点（含已调查状态）——按实际世界坐标与当前世界即时生成；
   - `surfaceWaypointById` 按稳定 id 解析（`surface:ship` / `surface:log:*` / `surface:anomaly:*`）。
2. **四级星图 UI**：
   - 地面按 **M** 打开“地球地表 // 地表导航”：画布以玩家为中心（340 格范围）显示青色玩家、琥珀飞船、青蓝日志、品红异常点；面包屑新增第四级“地球地表”；列表展示全部航点（名称/状态/距离）；
   - 行星系统图新增“进入地表导航”按钮（仅当前天体）；返回链：地表 → 行星系统 → 太阳系 → 恒星级。
3. **步行导航 HUD**：
   - 新增青色菱形**地表导航罗盘**（与太空目标罗盘/飞船罗盘分离）：显示航点名称与实时距离；
   - 选择航点后步行跟随罗盘；抵达 <3m 自动提示“已抵达”并清除目标；
   - 已调查航点禁止重设目标；切换世界自动清除失效地表目标。
4. **测试**：新增 `tools/verifysurface-nav.mjs`（7 项）：航点数据完整性 → 四级星图打开/面包屑/画布/列表 → 异常点设目标 → 罗盘名称与距离 → 抵达自动清除 → 层级返回。

### 验证
- `node tests/run.js`：**44492 通过，0 失败**（逻辑无回退）。
- `tools/verifysurface-nav.mjs`：**7 项全过**，`ERRORS: none`。
- 回归：`verifyfullrun`（17 步通关）、`verifypanels`、`verifyuniverse`、`verifyplanets`、`verifymoon`、`verifyjovian`、`verifybelt`、`verifymilestones`、`verifysolar`、`verifyproxima`、`verifystation` 全部通过。

### 下一轮候选
- 地表地图的可探索迷雾/已发现地标持久化（存档记录）；
- 地表航点筛选（只显示未收集/按类型过滤）与任务自动设目标；
- 恒星图扩展第三恒星系统。

## 第 38 轮（已完成）：地表探索迷雾与发现持久化——地表导航不再剧透

### 目标
四级星图里的“地表导航”不应开局把整颗行星的异常点全部剧透。本轮引入探索迷雾：未发现的异常点只在靠近（90m）或扫描后进入地图；发现状态随存档保存，形成“探索 → 发现 → 导航 → 抵达”的完整循环。

### 内容
1. **探索迷雾**（`Game.surfaceWaypoints({ includeHidden })` / `discoverSurfaceAround(radius)`）：
   - 飞船与数据日志始终可见；异常点仅在**靠近 90m / 扫描（60m）/ 已调查**后进入地表图；
   - 步行时低频自动发现（每 12 帧）；C 键扫描在现有品红信号之外同步点亮附近航点；
   - 新发现时提示“地表航点已发现 ×N · 按 M 查看地表导航图”。
2. **地图反馈**：
   - 地表图标题显示发现进度：`已发现 x/y 异常点`；当前位置信息卡说明“靠近或扫描可发现”；
   - 未发现异常点不进入画布节点、列表与目标选择（`surfaceWaypointById` 只解析可见航点）。
3. **存档持久化**：存档新增 `discoveredSurface` 字段；重载后探索迷雾与发现进度完整恢复（旧存档缺字段 → 空集，兼容）。
4. **测试**：`tools/verifysurface-nav.mjs` 扩至 **9 项**：新增“迷雾隐藏未发现航点”“发现半径揭示航点”“发现进度存档重载持久化”。

### 验证
- `node tests/run.js`：**44492 通过，0 失败**（无逻辑回退）。
- `tools/verifysurface-nav.mjs`：**9 项全过**（初始可见 6/17 → 迷雾隐藏 13 个异常点 → 发现 13 → 全部可见 → 存档重载 13/13 恢复），`ERRORS: none`。
- 回归：`verifyfullrun`、`verifypanels`、`verifyuniverse`、`verifyplanets`、`verifymoon`、`verifyjovian`、`verifybelt`、`verifymilestones`、`verifysolar`、`verifyproxima`、`verifystation` 全部通过。

### 下一轮候选
- 航点筛选（只显示未收集/按类型）与任务自动设目标；
- 地表地图高亮“已调查/已发现”之外的资源点类型；
- 恒星图扩展第三恒星系统。

## 第 39 轮（已完成）：全量验证审计——26 套浏览器套件全绿 + 两个真实缺陷修复

### 目标
功能扩张多轮后，必须按“持续运行游戏判断”的原则做一次完整审计，而不是只跑关键回归。本轮实际运行全部 26 个 `tools/verify*.mjs` 浏览器套件，发现并修复 2 个真实问题。

### 审计发现与修复
1. **比邻星系误触跃迁（真实游戏缺陷）**：真实轨道比例下，比邻星 d（0.029 AU ≈ 28.9 单位）与母星 b 距离小于两行星视觉球体直径，进入比邻星系后会被立即误判“接近行星 d”而自动跃迁。修复：
   - `generateSystem` 增加 `MIN_PLANET_GAP=150`：超紧凑行星按最小视觉间距展开（太阳系所有行星间距 >336 单位，不触发；现实 AU 数据仍保留在信息面板）；
   - `SpaceSystem.warpGrace=1.2s`：进入太空/切换星系后的跃迁保护（只作用于行星/卫星误触，不拦截玩家主动前往的跃迁门）。
2. **verifyintro 机器相关脆弱断言**：自然播完测试假设单帧 dt≥5ms，快速机器一帧仅 3.6ms 导致 5.495s < 5.5s 判定失败。改为循环驱动镜头直到自然结束（测试更健壮，游戏无缺陷）。

### 全量验证结果
- `node tests/run.js`：**44492 通过，0 失败**。
- **26/26 浏览器套件全部通过**（exit 0，0 页面错误）：verifyanomaly / belt / combat / firstplay / flight / fullrun / intro / inventory / jovian / milestones / moon / npc / panels / planets / proxima / round / roundtrip / savetransfer / solar / station / stormsoak / surface-nav / surface / survival / universe / weather。
- 全套审计耗时约 2 分 20 秒；修复后连续重跑 `verifyroundtrip`（4 次跨星系往返+存读档）与 `verifyintro`（17 项）均全过。

### 文档口径修正
- `tools/` 目录共 47 个脚本，其中 **26 个 `verify*` 无头浏览器回归套件**；README/FINAL-REPORT 的“35 套”计数口径已修正为 26（另有 perf/perflow/soak/审计与截图工具）。

### 下一轮候选
- 恒星图扩展第三恒星系统（框架已可扩展，补数据 + 第二跃迁门）；
- 航点筛选与任务自动设目标；
- 行星地下结构差异化（热行星熔岩洞/冰行星晶洞）。

## 第 40 轮（已完成）：恒星图第三节点——天狼星系统（三星系宇宙 + 多跃迁门）

### 目标
第 32 轮建立的恒星级星图框架此前只有两个节点。本轮用**真实数据驱动的第三恒星系统**证明框架可扩展：太阳系、比邻星系、天狼星系构成三星系宇宙，太阳系拥有两条跨星系跃迁门。

### 内容
1. **天狼星数据**（`celestial.js`，纯数据扩展）：
   - `STAR_SYSTEMS` 新增天狼星系：A1V 蓝白主序星、8.6 光年、2 颗“推测行星”（0.49 AU 灼热荒漠 / 4.2 AU 冰封外缘，均带完整地形/资源/危险/色板）；
   - `STARS` 天狼星节点 + `GATEWAY_NODES` 双向门（`gateway:solar.sirius` / `gateway:sirius.solar`，各自独立世界偏移）；
   - `planetsOfSystem / homeBodyId / systemLevelNodes / starColorOf` 全部泛化为按系统 id 解析，未来新增恒星系统只加数据。
2. **太空运行时多跃迁门**（`system.js`）：
   - `gatewayGroups` 数组替代单一 `gatewayGroup`：太阳系同时实体化“比邻星门”与“天狼星门”，独立检测/跃迁；旧 `gatewayGroup` 保留为主门兼容工具脚本；
   - `visitedFor(systemId)` + `visitedSirius`：星系访问记录按系统数据驱动；切换星系用 `systemMeta` 命名，不再硬编码“太阳系/比邻星系”；
   - 跨星系跃迁里程碑改为“任意非太阳系目标”触发。
3. **星图 UI**：恒星级三节点布局（太阳/比邻/天狼 + 双航线）；太阳系系统图与旧列表都出现第二个门（保留 200/201 数字兼容，新门用字符串 navId）。
4. **存档**：新增 `visitedSirius`，旧存档兼容；天狼星系读档恢复星系/行星/访问记录。
5. **测试**：新增 `tools/verifysirius.mjs`（8 项）：三星图 → 太阳系双门 → 天狼星门目标/跃迁 → 天狼星 c 登陆 → 存档重载 → 从新页面经反向门返回太阳系。

### 验证
- `node tests/run.js`：**44496 通过，0 失败**（新增 4 项：三星系/8.6 光年/天狼星两行星/双向门入册）。
- `tools/verifysirius.mjs`：**8 项全过**，`ERRORS: none`。
- 全量审计：**27/27 无头浏览器套件全绿**（比上轮 +verify sirius）。
- 关键回归：`verifyfullrun`、`verifyproxima`、`verifyroundtrip` 全部通过。

### 下一轮候选
- 恒星图第四节点（如巴纳德星）或天狼星白矮星伴星视觉；
- 航点筛选与任务自动设目标；
- 行星地下结构差异化（热行星熔岩洞/冰行星晶洞）。

## 第 41 轮（已完成）：行星地下结构差异化——热行星熔岩囊 / 冰世界晶洞

### 目标
目标清单里的“地下结构”此前只有统一的洞穴噪声。本轮让不同天体的地下世界真正不同：热行星挖到地壳浅层会遇见熔岩，冰世界洞穴地面生长二氢晶簇，地球保持熟悉的矿物结构。

### 内容
1. **新方块**：`MAGMA`（地热熔岩，22 号）：暗色岩底 + 橙黄熔岩裂纹像素纹理；硬度 2.6（比普通岩石难挖），挖掘掉落岩石（不新增背包物品，避免膨胀）。
2. **地下结构生成**（`chunk.js` 2.5 阶段，确定性、不破坏地表）：
   - **magma 世界**（水星 / 金星 / 木卫一 / 天狼星 b）：地壳 16% 深度起生成熔岩囊（双层概率、受洞穴影响但不覆盖空气）；
   - **crystal 世界**（surface='ice' 自动启用，或数据显式指定）：洞穴地面按概率生成**二氢晶簇**——冰世界的洞穴本身成为资源点；
   - 地球/火星等无特殊地下结构，保持原世界感。
3. **数据贯通**：行星/卫星定义新增 `underground` 字段，经注册表 → generateSystem → applyNewWorld → WorldGen 全链路传递；`WorldGen` 构造器完成 ice→crystal 推导。
4. **测试**：新增 `tools/verifyunderground.mjs`（3 项）：地球 0 熔岩 / 水星熔岩 7257 块（5×5 区块）/ 木卫二晶簇 1434 块。

### 验证
- `node tests/run.js`：**44502 通过，0 失败**（新增 6 项：热行星熔岩囊、冰世界晶簇、地球无熔岩、新方块定义完整性）。
- `tools/verifyunderground.mjs`：**3 项全过**，`ERRORS: none`。
- 全量审计：**28/28 无头浏览器套件全绿**（新增 verifyunderground）。
- 关键回归：`verifyfullrun`、`verifyplanets`、`verifymoon`、`verifyjovian`、`verifybelt`、`verifyuniverse`、`verifysirius` 全部通过。

### 下一轮候选
- 熔岩环境危险（靠近熔岩受伤/发光）+ 冰晶洞光照氛围；
- 航点筛选与任务自动设目标；
- 恒星图第四节点（如巴纳德星）。

## 第 42 轮（已完成）：熔岩环境危险与动态火光

### 目标
第 41 轮铺好了热行星熔岩囊，但熔岩只是会发亮图案的硬石头。本轮让它真正“热起来”：靠近会烫伤、洞窟里有摇曳的火光，让水星/金星/木卫一/天狼星 b 的浅层地壳成为危险与回报并存的地带。

### 内容
1. **灼烧伤害**（`game.js`）：
   - 玩家贴近岩浆方块（±2/±1/±3 范围，脚下 1 层到头顶 3 层）时每秒受到 3 点伤害，护盾按 `player.damage` 规则吸收一半；每 0.5 秒刷新一次检测，飞行/创造模式免疫；
   - 首次被烫弹出一次性提示，并在岩浆旁触发屏幕灼热红光反馈。
2. **动态火光**（`game.js`）：
   - 岩浆扫描器每 0.5 秒在玩家周围 ±13/±9/±5 范围内寻找熔岩块，最多点亮 8 盏 `0xff7a20` 橙色点光源，带随机脉动，让热行星洞窟不再漆黑；
   - 进入载具/跃迁/换世界时自动清灯；`applyNewWorld` 统一清理上一世界的光源与灼烧状态。
3. **测试**：扩展 `tools/verifyunderground.mjs`（3 → 5 项）：熔岩灼伤把 98.5 血烧到 97 并触发警告 / 动态火光 8 盏全亮。

### 验证
- `node tests/run.js`：**44502 通过，0 失败**。
- `tools/verifyunderground.mjs`：**5 项全过**，`ERRORS: none`（含新熔岩灼伤与动态火光检查）。
- 全量审计：**28/28 无头浏览器套件全绿**。
- 关键回归：`verifyfullrun`、`verifyplanets`、`verifymoon`、`verifybelt`、`verifyuniverse`、`verifysirius` 全部通过。

### 下一轮候选
- 行星地表差异化深化（天空颜色已按大气密度变化，可继续：重力对跳跃手感、温度 HUD、天气粒子）；
- 冰晶洞光照氛围（低温幽光）；
- 恒星图第四节点（如巴纳德星）。

## 第 43 轮（已完成）：大气差异化——阻力与再入热障

### 目标
前几轮的大气密度只影响天空颜色、雾距与星空可见度。本轮把同一条真实 `atmoDensity` 数据接入**飞行物理与再入瞬间**：金星浓密大气拖慢飞船、再入时被等离子体长时包裹；火星稀薄大气阻力减半、热障短促；月球等无大气卫星真空滑翔、完全没有再入热障。玩家无需看任何 UI 就能感到“这是另一个世界”。

### 内容
1. **数据层纯函数**（`celestial.js`）：
   - `airDragFactor`：地球 1.0 / 金星 1.78 / 火星 0.73 / 无大气（密度 <0.3）0.15；
   - `reentryStrengthOf`：金星 1.0 / 地球 0.6 / 火星 0.375 / 月球 0；
   - `reentryDurationOf`：金星 2.6s / 地球 1.7s / 火星 1.3s / 月球 0（严格按密度排序）。
2. **飞行物理**（`shipflight.js`）：无油门阻力改为 `DRAG × airDragK`——真空世界几乎不减速，稠密大气明显拖慢；机尾橙红等离子粒子 + 机头白热闪光按热障强度发射，引擎辉光同步增强。
3. **再入触发**（`system.js`）：从太空回落到 130 高度以下时 `beginReentry()`，由当前天体密度决定“浓密/一般/稀薄/无大气”四种反馈（屏幕震动、警报音、toast）；进入太空或强制退出时清除状态。
4. **屏幕滤镜**（`ui.js`/`index.html`/`style.css`）：新增 `#reentry-overlay` 橙红边缘灼热光晕（中心透明、边缘渐强、闪烁动画），强度/时长随大气密度。
5. **测试**：新增 `tools/verifyatmo.mjs`（8 项）：阻力参数排序 / 实际物理回路月球减速仅为地球 15% / 地球再入等离子体 + 滤镜 / 热障自动熄灭 / 月球无热障；`tests/run.js` +4 断言。

### 验证
- `node tests/run.js`：**44506 通过，0 失败**（新增 4 项大气阻力/热障参数断言）。
- `tools/verifyatmo.mjs`：**8 项全过**，`ERRORS: none`；截图像素探针确认边缘 R 通道显著高于中心、G/B 被压制（灼热光晕成立）。
- 关键回归：`verifyplanets`/`verifymoon`/`verifyjovian`/`verifybelt`/`verifyuniverse`/`verifysirius`/`verifysurface-nav`/`verifyunderground` 全部通过。
- 全量审计：**29/29 无头浏览器套件全绿**（新增 verifyatmo）。

### 下一轮候选
- 环境温度系统：HUD 实时温度 + 极端温度屏幕反馈（热浪/霜冻），昼夜温差接入危险防护；
- 冰晶洞光照氛围（低温幽光，与熔岩火光配对的冰世界地下体验）；
- 恒星图第四节点（如巴纳德星）。

## 第 44 轮（已完成）：核心乐趣专项——回到“好玩”本身（移动手感 / 挑战完整性 / 新手指引）

### 目标
第 32–43 轮持续扩宽宇宙（卫星/星图/小行星/大气），功能表很满，但接管体验审计提出第一性原理问题：**游戏首先是好玩，不是首先“有很多系统”**。本轮先修最伤核心乐趣的三类问题：移动手感僵硬、生存玩法可被一键免费飞行/中途切创造短路、新玩家在合成面板里找不到当前任务要做什么。同时做一轮低风险材质观感提升。

### 内容
1. **移动手感（player.js）**：
   - **Ctrl 疾跑**：前进时速度 ×1.45（与潜行互斥），不设耐力条——移动爽感优先于写实管理；
   - **疾跑 FOV 冲击**：速度起来后 FOV 平滑拉宽 6.5%，停下自动回落；
   - **跳跃缓冲 + 土狼时间**：落地前 0.12s 内按跳跃会被记住、落地瞬间起跳；离开平台边缘后 0.09s 内仍可起跳——消灭“按了没跳/边缘吞跳”的平台手感问题。
2. **挑战完整性（生存玩法不被短路）**：
   - 生存模式 **X 不再开启免费飞行**（创造模式仍可飞）；菜单/HUD/README 同步去“开发辅助”痕迹；
   - **游戏进行中锁定创造模式切换按钮**：模式只能在主菜单选择，杜绝暂停菜单一键上帝模式。
3. **新手指引（quests.js/ui.js）**：
   - `Quests.requiredRecipeId()`：当前任务需要的配方（多功能工具/金属镀层/密封胶/玻璃/发射燃料）在背包合成面板中**置顶 + 琥珀色脉冲高亮 + “当前任务”徽章**；
   - 新玩家打开背包第一眼看到“现在该合成什么”，而不是在 9 个配方里找。
4. **首次入夜提示（game.js）**：夜幕降临时一次性提示“危险防护下降、采集钠花、搭庇护所避怪”，白昼重置——惩罚前先给可执行对策。
5. **材质观感（mesher.js/game.js）**：
   - 逐面微差：每个方块面按世界坐标获得 0.93–1.00 的确定性明暗差异，大面积地表/岩壁不再整片同亮度；
   - 纹理图集启用各向异性过滤（≤8×）；中等宽度视口下右下快捷键提示上移，避免与快捷栏重叠。
6. **测试**：新增 `tools/verifygamefeel.mjs`（6 项）：疾跑速度/FOV 冲击/生存禁飞/游戏内禁切创造/任务配方高亮/首次入夜提示；修复 `verifyintro` 音乐断言竞态。

### 验证
- `node tests/run.js`：**44512 通过，0 失败**（新增 6 项任务配方高亮断言）。
- `tools/verifygamefeel.mjs`：**6 项全过**，`ERRORS: none`。
- 关键回归：`verifyfullrun`（17 步通关）、`verifyintro`、`verifyinventory`、`verifyround` 全部通过。
- 浏览器套件总数提升至 **30 套**；接管环境使用 Chrome for Testing 151 复跑全量审计（详见工具日志）。

### 下一轮候选（按“好玩”优先级，不再按功能表优先级）
- 挖掘/建造深度：采矿光束升级、多格挖掘、方块旋转与更多建材；让“采集建造”不只是任务过场；
- 战斗可读性：怪物血条/受击硬直/命中标记/武器升级，让夜间战斗有来有回；
- 经济节奏：空间站升级成本、可重复订单与里程碑信用点奖励联动，把“刷矿卖钱”变成“探索赚钱”；
- 材质第二轮：32px 重绘 + 方块纹理变体 + 水体表现（当前最大视觉空洞）。

## 第 45 轮（已完成）：采矿与建造深度——工具升级 / 3×3 区域开采 / 放置虚影 / 石砖

### 目标
第 44 轮把“移动、挑战、指引”的基础手感修好之后，本轮按体验审计的优先级回到**游戏性最长板候选：采矿与建造**。此前多功能工具只让挖速 ×3.2，之后 40 分钟内容里采矿没有任何变化；建造也缺少“放之前看见结果”的反馈。

### 内容
1. **工具升级线**：
   - 新增 **采矿光束 MkII**（`mining_beam_mk2`，采矿速度 ×5.2）：配方 = 多功能工具 + 铜矿石×4 + 金矿石×2 + 强化玻璃（MkI 被消耗，形成真实升级而非无限堆叠）；
   - 空间站可 800 信用点直接购买；空间站价格表同步注册；
   - 新增第 20 项里程碑 **“矿脉切割者”**：获得 MkII 即点亮，存档反推物品自动补记。
2. **R 键采矿模式**：MkII 专属 **3×3 区域开采**——按瞄准面法线取平面（顶面=水平 3×3，侧面=竖直 3×3）；9 块一起充能一起碎，充能时间按命中块数量加收（不是无成本清场）；MkI/徒手按 R 会得到明确的成长提示。
3. **挖掘反馈升级**：高亮框随挖掘进度**收缩并从青色烧向橙红**，不用看准星环也知道“快碎了”；区域开采掉落按物品聚合弹窗，不会 9 条刷屏。
4. **建造反馈**：新增**预放置虚影**——手持可放置方块并瞄准表面时显示半透明方块，可放=绿色、不可放=红色；`placeBlock` 现在返回是否成功，方便测试与后续自动建造。
5. **新建材**：新增 **石砖**（4 岩石 → 4 石砖），独立砌缝纹理；可放置/可售卖/可回购。
6. **HUD 工具档位**：准星下方常驻“徒手开采 / 多功能工具 · 单格开采 / 采矿光束 MkII · 单格精采 / 区域 3×3”。
7. **测试**：新增 `tools/verifybuilddepth.mjs`（6 项）：物品注册与 11 配方表 / 石砖虚影与放置 / MkI 无区域模式 + MkII 合成消耗 MkI + 里程碑 / 顶面 3×3 一次破坏 9 块 / 高亮烧红收缩 + HUD 档位 / 存档重载恢复。

### 验证
- `node tests/run.js`：**44540 通过，0 失败**（新增 28 项：石砖/MkII 配方、工具等级、20 项里程碑等）。
- `tools/verifybuilddepth.mjs`：**6 项全过**，`ERRORS: none`。
- 关键回归：`verifyfullrun`（17 步通关）、`verifymilestones`（20 项面板）、`verifygamefeel`、`verifyinventory`、`verifystation`、`verifyweather` 全部通过。
- **全量审计 31/31 无头浏览器套件全绿**（`verifystormsoak` 15 分钟风暴战斗长测在内），`ERRORS: none`。

### 下一轮候选（继续按“好玩”优先级）
- 战斗可读性与来回：怪物血条/受击硬直/命中标记/武器模块，让夜间战斗不再只是“点 Q 换血”；
- 经济正循环：里程碑/探索奖励信用点、可重复订单冷却与收益重平衡，把“刷矿卖钱”改成“探索赚钱”；
- 建造的安全感与占有感：基地边界/床铺/重生点/储物箱；
- 材质第二轮：32px 重绘 + 方块纹理变体 + 水体表现。

## 第 46 轮（已完成）：战斗可读性与来回——血条 / 前摇 / 硬直 / 闪避窗口 / 命中标记 / 能量线圈

### 目标
体验审计指出战斗此前是“带眼睛的盒子 + 点 Q 换血”：没有血条、没有前摇、没有受击反馈，危险只靠数值。本轮把夜间战斗从“站桩互殴”升级为有信息、有节奏、有闪避收益的短兵相接。

### 内容
1. **怪物血条**（`mobs.js`）：头顶实时血条，满血隐藏、受击后出现，绿（>55%）/橙（>25%）/红（≤25%）三档变色；始终朝向相机、穿透可见，不因草丛/角度丢失信息；销毁时释放几何与材质。
2. **攻击前摇**：三种怪物各自前摇（异虫 0.22s / 夜行兽 0.30s / 岩甲爬兽 0.50s）——进入攻击范围先亮红眼 + 警示音，前摇期间移速降至 38%；前摇结束**再次校验**距离/高低/视线。
3. **闪避有收益**：玩家在前摇中退开或绕到墙后，怪物挥空并进入 45% 短后摇，创造反击窗口；命中则正常伤害 + 击退 + 屏幕震动。
4. **受击硬直**：能量弹命中让怪物硬直 0.18s、移速降至 12%，并**打断攻击前摇**——连射可以压制敌人，但换弹节奏与走位仍重要。
5. **命中标记**（`ui.js`/`index.html`/`style.css`）：能量弹命中敌人时，准星四角弹出琥珀短斜线（130ms 自动消失），配合音效形成完整命中确认。
6. **武器模块：能量线圈**（新物品/配方/图标/空间站购买）：
   - 铜矿石×3 + 碳×4 + 金矿石×1 合成，或空间站 450 信用点购买；
   - 伤害 14 → 22（+57%），射击冷却 0.32s → 0.24s（+33%），弹丸/命中粒子由青变橙；
   - 新增第 21 项里程碑 **“能量武器匠”**；存档重载自动反推点亮。
7. **测试**：新增 `tools/verifycombatdepth.mjs`（8 项）：血条显隐与比例 / 硬直打断前摇并减速 / 前摇期间不扣血且结算扣血 / 前摇中退开闪避 + 短后摇 / 命中标记 / 线圈数值 / 存档恢复 / 页面错误。

### 验证
- `node tests/run.js`：**44554 通过，0 失败**（新增 14 项：能量线圈配方/模块注册/第 21 项里程碑）。
- `tools/verifycombatdepth.mjs`：**8 项全过**，`ERRORS: none`。
- 旧战斗套件 `verifycombat`：**14 项全过**（不穿墙/隔墙不攻击/高处安全/白天燃烧/追击压迫/种类差异全保留）。
- 关键回归：`verifyfullrun`、`verifymilestones`（21 项面板）、`verifyinventory`、`verifystormsoak`（15 分钟风暴战斗长测）全部通过。
- **全量审计 32/32 无头浏览器套件全绿**（含风暴战斗长测与全部旧套件），`ERRORS: none`。

### 下一轮候选（继续按“好玩”优先级）
- 经济正循环：里程碑/探索奖励信用点、可重复订单冷却与收益重平衡，把“刷矿卖钱”改成“探索赚钱”；
- 基地占有感：床铺/重生点/储物箱/基地边界；
- 材质第二轮：32px 重绘 + 方块纹理变体 + 水体表现；
- 飞船战斗或太空威胁（陨石/海盗）——战斗深度从地表延伸到太空。

## 第 47 轮（已完成）：经济正循环——里程碑发信用点 / 可重复订单 / 飞船线节奏重平衡

### 目标
体验审计指出大船线合计 2350 信用点，主要靠“刷矿卖钱”；里程碑只给称号不给资源，探索与赚钱脱节。本轮把经济从“挖矿打工”改为“探索自然变富”：玩家做的每件事都在推进里程碑，里程碑直接发钱；空间站订单可重复但有节奏；飞船线价格降到主线探索+少量交易即可负担。

### 内容
1. **里程碑信用点奖励**（`milestones.js`）：
   - 全部 21 项里程碑新增 `reward`（5–400 信用点）：探索/飞行/登月/小行星/击杀/建造/工具升级都有钱；
   - `Milestones.earn` 直接发放入背包并计入 `creditsEarned`，可连锁点亮“太空商人”里程碑；
   - 背包满额时保留里程碑、提示“奖励暂缓”，不吃奖励；
   - 旧存档不追溯奖励（不会读档突然刷钱），新进度即时发放。
2. **可重复收购订单**（`station.js`/`game.js`/`ui.js`）：
   - 订单由“一次性奖励”改为 **可重复提交，每单 45 秒冷却**；
   - UI 显示冷却倒计时，冷却中按钮禁用；点击不吞材料/信用点；
   - 冷却随主循环自然衰减，停靠时订单页每秒刷新；存档保存冷却状态。
3. **飞船线价格重平衡**：主线必需升级（引擎 Lv1 150 + Lv2 350 + 护盾 Lv1 100 + 曙光号 800）= **1400 信用点**；主线里程碑约 855 + 少量交易即可，完整探索（登月/卫星/小行星/日志/异常点）几乎零挖矿。
4. **测试**：新增 `tools/verifyeconomy.mjs`（6 项）：里程碑发钱与 merchant 连锁 / 订单冷却与按钮状态 / 冷却中不吞货不吞钱 / 冷却归零可再交 / 主线价 1400 / 存档恢复。

### 验证
- `node tests/run.js`：**44561 通过，0 失败**（新增 7 项：奖励入账、merchant 连锁、订单冷却常量、主线价 1400、存档冷却往返）。
- `tools/verifyeconomy.mjs`：**6 项全过**，`ERRORS: none`。
- 关键回归：`verifyfullrun`（17 步通关）、`verifymilestones`（21 项面板）、`verifystation`、`verifyinventory`、`verifygamefeel`、`verifycombatdepth` 全部通过。
- **全量审计 33/33 无头浏览器套件全绿**（含风暴战斗长测与全部旧套件），`ERRORS: none`。

### 下一轮候选（继续按“好玩”优先级）
- 基地占有感：床铺/重生点/储物箱/基地边界，让建造真正“属于玩家”；
- 材质第二轮：32px 重绘 + 方块纹理变体 + 水体表现；
- 太空威胁与飞船战斗：陨石带风险/海盗拦截/舰载护盾与武器；
- 行星任务板：空间站可接取多样任务（不只是三单收购）。

## 第 48 轮（已完成）：基地占有感——基地终端/重生点/休息/安全区/储物箱

### 目标
体验审计指出建造目前是“放方块过任务”，没有“这是我的家”的占有感。本轮补上基地最小闭环：**能安家、能回家、能休息、能存东西**。基地与储物箱按世界隔离、随存档保存；死亡不再总是被扔回坠机点。

### 内容
1. **基地终端**（新方块/物品/配方/纹理，B.BASE_UNIT）：
   - 配方：金属镀层×2 + 强化玻璃×1 + 碳×4（或空间站 160 信用点购买）；
   - 放置后立即记录**重生点**，基地上方出现蓝青信标光；
   - 新增第 22 项里程碑 **“安家落户”**（奖励 100 信用点）。
2. **基地休息**：靠近终端按 E——白天确认重生点；**夜晚休息到清晨**并回满生命/护盾/生命维持/危险防护。家从此有真实功能。
3. **基地安全区**：终端半径 **28m 内不刷怪**，群居异虫的成队个体也会被安全区过滤，不会从队形漂移漏进家。
4. **死亡回家**：有基地时死亡从终端旁重生（无基地仍回坠机点）。
5. **储物箱**（新方块/物品/配方/纹理，B.STORAGE，12 格）：
   - 配方：木制板材×4 + 金属镀层×1（或空间站 90 信用点购买）；
   - 靠近按 E 打开面板：**单格取出 / 全部取出 / 背包区全部存入**（快捷栏永不误存）；
   - 纯逻辑模块 `src/systems/storage.js`：堆叠、跨格存放、序列化/恢复、老存档兼容。
6. **持久化**：基地与储物箱按 `seed|galaxy|planet|body` 世界键隔离保存；读档/换世界后按实体坐标**自动重建方块**，信标与箱子内容恢复。
7. **测试**：新增 `tools/verifybase.mjs`（7 项）：物品/配方注册、基地放置与信标、休息到清晨、基地重生、28m 安全区、储物箱存取与面板状态、存档世界重建。

### 验证
- `node tests/run.js`：**44605 通过，0 失败**（新增 44 项：储物箱纯逻辑、基地/储物配方、第 22 项里程碑、存档地图恢复）。
- `tools/verifybase.mjs`：**7 项全过**，`ERRORS: none`。
- 关键回归：`verifyfullrun`（17 步通关）、`verifymilestones`（22 项面板）、`verifycombat`、`verifybuilddepth`、`verifyeconomy`、`verifystation`、`verifyinventory` 全部通过。
- **全量审计 34/34 无头浏览器套件全绿**（含风暴战斗长测与全部旧套件），`ERRORS: none`。

### 下一轮候选（继续按“好玩”优先级）
- 材质第二轮：32px 重绘 + 方块纹理变体 + 水体表现（视觉最大空洞）；
- 太空威胁与飞船战斗：陨石带风险/海盗拦截/舰载护盾与武器；
- 行星任务板：空间站接取更多样任务（探索悬赏/送货/猎杀）；
- 基地继续深化：床铺视觉/命名/多基地列表。

## 第 49 轮（已完成）：水体视觉与浅水玩法——湖泊 / 半透明流体网格 / 涉水 / 深水缺氧 / 水上建造

### 目标
体验审计最大的视觉空洞是“无水体表现”：类地行星没有湖海，世界感缺一大块。本轮补齐水体，并让水不只是贴图——涉水减速、深水有窒息风险、水上可填土建造。

### 内容
1. **水体方块**（B.WATER，id 26）：透明、非固体、不可挖掘；独立 `TILE.WATER` 半透明蓝色波痕纹理。
2. **湖泊生成**（`chunk.js`）：`DEFAULT_TERRAIN.waterLevel = 14`，草地行星高度低于水面的洼地自动灌水至水面（确定性；非草地行星同位置保持干燥）。测试种子中低洼区块生成 82–147 个水体块。
3. **独立流体网格**（`mesher.js`/`world.js`/`game.js`）：
   - `buildChunkGeometry` 新增 `fluid` 缓冲，只生成暴露于空气/植物旁的水面，水-水相邻面互相剔除；
   - 新增第三套材质 `fluidMat`（透明 Lambert，depthWrite=false）；区块增加 `meshFluid`，卸载/隐藏/切世界全链路管理；
   - 修复水邻接查询 bug：邻居坐标必须基于当前局部列，否则水面全部被错误剔除。
4. **涉水玩法**（`player.js`）：
   - 脚部入水移动速度 ×0.55（创造/飞行不受影响）；
   - 头部没入水时生命维持以 1.8 倍速度消耗，低于 25 提示“深水缺氧”，耗尽后每 0.9s 受伤；
   - 预放置虚影允许把方块放在水格（绿色），右键可直接填水建造。
5. **测试**：新增 `tools/verifywater.mjs`（5 项）：水体注册与透明材质 / 草地湖泊与流体网格 / 涉水减速 / 深水先警告后受伤 / 水上填方建造；`tests/run.js` 新增水体生成与流体几何断言。

### 验证
- `node tests/run.js`：**44614 通过，0 失败**（新增 9 项：湖泊生成、非草地干燥、流体 UV/法线等）。
- `tools/verifywater.mjs`：**5 项全过**，`ERRORS: none`。
- 关键回归：`verifyfullrun`、`verifymilestones`、`verifybase`、`verifybuilddepth`、`verifycombat`、`verifyeconomy`、`verifysurface`、`verifyweather` 全部通过。
- **全量审计 35/35 无头浏览器套件全绿**（含风暴战斗长测与全部旧套件），`ERRORS: none`。

### 下一轮候选（继续按“好玩”优先级）
- 材质第二轮：32px 重绘 + 方块纹理变体（水体补上后剩余的最大视觉重复感）；
- 太空威胁与飞船战斗：陨石带风险/海盗拦截/舰载护盾与武器；
- 行星任务板：空间站接取更多样任务（探索悬赏/送货/猎杀）；
- 基地深化：床铺视觉/基地命名/多基地列表。

## 第 50 轮（已完成）：材质变体——常见方块按世界位置换纹，打破大面积重复

### 目标
水体补上后，地表最大的观感问题是同一种方块整片使用同一张 16px 纹理。本轮在不改变体素风格、不增加显存负担的前提下，给最常见方块加入**确定性材质变体**：同一方块按世界坐标从 2–3 张纹理中选一张，大面积地表自然形成色斑与纹理过渡。

### 内容
1. **图集扩展 38 → 50 瓦片**（`tiles.js`）：新增草顶 ×2、草侧 ×1、泥土 ×2、岩石 ×2、沙 ×2、红沙 ×1、雪 ×1、树叶 ×1。
2. **方块变体池**（`blocks.js`）：GRASS/DIRT/STONE/SAND/LEAVES/RED_SAND/SNOW 增加 `variants: { top|side|bottom|all }`；每池 2–3 瓦片。
3. **确定性选择**（`mesher.js`）：新增导出 `pickTileVariant(def, face, wx, y, wz)`——用世界坐标哈希在变体池中选纹；同一方块重网格化/换设备结果一致。
4. **侧纹理不变量保持**：草皮侧变体仍遵循“高顶点取瓦片顶 v、低顶点取底 v”，历史朝向 bug 回归测试改为验证每四边形内的高/低 v 分组（兼容不同图集行）。
5. **测试**：新增 `tools/verifytexturevariants.mjs`（3 项）：50 瓦片图集与变体池 / 96 位置实际使用多个变体且确定 / 变体画布像素互不相同。

### 验证
- `node tests/run.js`：**44651 通过，0 失败**（新增 37 项：变体池完整性、确定性选择、多位置使用、草侧朝向兼容）。
- `tools/verifytexturevariants.mjs`：**3 项全过**，`ERRORS: none`。
- 关键回归：`verifyfullrun`、`verifywater`、`verifybase`、`verifybuilddepth`、`verifysurface`、`verifymilestones` 全部通过。
- **全量审计 36/36 无头浏览器套件全绿**（含风暴战斗长测与全部旧套件），`ERRORS: none`。

### 下一轮候选（继续按“好玩”优先级）
- 太空威胁与飞船战斗：陨石带风险/海盗拦截/舰载护盾与武器；
- 行星任务板：空间站接取更多样任务（探索悬赏/送货/猎杀）；
- 基地深化：床铺视觉/基地命名/多基地列表；
- 生物视觉与行为多样性：爬兽/虫群外观差异、稀有精英怪。

## 第 51 轮（已完成）：太空威胁与舰载战斗——微陨石 / 海盗拦截机 / Q 舰炮 / 紧急返航

### 目标
战斗深度此前只在地表；小行星带 E 键开采是无风险提款机。本轮把风险与战斗延伸到太空：小行星带会有微陨石撞击，附近刷新海盗拦截机，飞船获得护盾/船体与 Q 舰炮，船体归零自动紧急返航。

### 内容
1. **舰船生存**（`shipflight.js`）：新增 `shield/shieldMax/hull/hullMax`；护盾升级同步提高舰船护盾上限；`takeShipDamage` 先盾后船体，带受击冷却/低船体警告/屏幕震动；安全空域护盾与船体缓慢回复。
2. **舰船 HUD**（`index.html`/`ui.js`/`style.css`）：飞行 HUD 新增舰船护盾与船体双条（青色护盾/橙色船体），随 `updateHUD` 低频刷新。
3. **太空战斗系统**（新 `src/space/spacecombat.js`）：
   - **微陨石**：小行星带内每 2–4.4s 随机撞击 6–13 伤害；
   - **海盗拦截机**：小行星带附近每 9–16s 刷新（最多 2 架），保持 40m 距离追击、侧向漂移、2.1s 间隔发射红色能量弹；
   - **Q 舰炮**：驾驶中发射 95m/s 舰载弹，高速弹丸采用“移动前+移动后”双重命中检测，避免单帧穿透；残血海盗可一炮击毁；
   - **击毁奖励**：60–100 信用点赏金 + 第 23 项里程碑 **“深空王牌”**；
   - 离开危险区/跃迁/返航时清场。
4. **紧急返航**：船体归零自动退出太空并降落在当前星球，护盾/船体回满，玩家恢复地面控制。
5. **存档**：舰船护盾/船体随存档保存并恢复。
6. **测试**：新增 `tools/verifyspacecombat.mjs`（7 项）：HUD 双条 / 先盾后船体 / 微陨石伤害 / 安全空域回复 / 海盗刷新与舰炮击杀奖励 / 紧急返航 / 存档恢复。

### 验证
- `node tests/run.js`：**44654 通过，0 失败**（新增 3 项：第 23 项里程碑、舰船护盾/船体存档往返）。
- `tools/verifyspacecombat.mjs`：**7 项全过**，`ERRORS: none`。
- 关键回归：`verifyfullrun`、`verifymilestones`（23 项面板）、`verifyflight`、`verifybelt`、`verifystation`、`verifybase` 全部通过。
- **全量审计 37/37 无头浏览器套件全绿**（含 15 分钟风暴战斗长测；软件渲染环境下 `verifystormsoak` 单独重跑通过），`ERRORS: none`。

### 下一轮候选（继续按“好玩”优先级）
- 行星任务板：空间站接取更多样任务（探索悬赏/送货/猎杀）；
- 基地深化：床铺视觉/基地命名/多基地列表；
- 生物视觉与行为多样性：稀有精英怪/种群差异；
- 飞船武器升级：舰炮伤害/射速模块，与能量线圈形成地面/太空双线成长。

## 第 52 轮（已完成）：行星任务板——探索悬赏 / 送货 / 猎杀 / 采矿

### 目标
空间站此前只有三单收购，缺少“去哪、做什么、拿什么”的长期目标供给。本轮新增任务板，把玩家已经在做的事（探索、采集、战斗、挖矿）变成可接取、可完成、可重复的悬赏循环。

### 内容
1. **任务板数据**（新 `src/systems/missions.js`）：6 个悬赏模板——火星/月球探索悬赏、铁氧体/金矿送货、夜行兽猎杀、40 方块采矿；纯函数 `createMission / missionEvent / missionReady / missionOffers`。
2. **空间站“任务板”页**：每次提供 3 个不同悬赏；接受后 HUD 顶部显示“悬赏 · 名称 · 进度”；完成后 60 秒冷却才能接下一单；可放弃（同样进入冷却）。
3. **自动进度挂钩**：
   - 挖掘任意方块 → 采矿悬赏 +1；
   - 击杀夜行兽 → 猎杀悬赏 +1（错误生物不计数）；
   - 降落到指定行星/卫星 → 探索悬赏完成（错误天体不计数）；
   - 送货悬赏在任务板点击交付，材料不足/背包满不吞货。
4. **奖励**：完成直接发 80–150 信用点并计入经济里程碑；任务状态、冷却、轮换种子随存档保存。
5. **测试**：新增 `tools/verifymissionboard.mjs`（6 项）：3 悬赏 UI 与 HUD / 采矿自动计数完成 / 猎杀指定生物 / 访问指定天体 / 送货交付 / 存档恢复。

### 验证
- `node tests/run.js`：**44664 通过，0 失败**（新增 10 项：模板/事件推进/offer 去重/存档任务字段）。
- `tools/verifymissionboard.mjs`：**6 项全过**，`ERRORS: none`。
- 关键回归：`verifyfullrun`、`verifymilestones`、`verifystation`、`verifyspacecombat`、`verifybase` 全部通过。
- **全量审计 38/38 无头浏览器套件全绿**（含 15 分钟风暴战斗长测与全部旧套件），`ERRORS: none`。

### 下一轮候选
- 基地深化：床铺视觉/基地命名/多基地列表；
- 自然选择号模型整合：工作区已有程序化模型与 GLB，可作为曙光号外观/空间站展示；
- 生物视觉与行为多样性：稀有精英怪/种群差异；
- 飞船武器升级：舰炮伤害/射速模块。

## 第 53 轮（已完成）：KNOWN-ISSUES 严格 Review 修复收口——P0–P3 按序清零

### 目标
把 `KNOWN-ISSUES.md` 中 Review 确认但未修复的 8 个问题按 P0 → P1 → P2 → P3 顺序全部修复，并补齐对应回归，避免“已确认问题长期挂账”。

### 内容
1. **P0-1 水体生成顺序**（`chunk.js`）：植物/树木阶段跳过草地湖泊水面以下列，湖水不再被钠花/氧草/二氢/树木覆盖。
2. **P1-2 里程碑奖励补发**（`milestones.js`/`game.js`）：新增 `pending` 队列与 `flushPendingRewards()`，`Game.loop` 每帧检查；背包有空位后自动补发信用点，pending 随存档保存。
3. **P1-3 猎杀悬赏语义**（`mobs.js`/`missions.js`）：白昼自燃死亡不再推进猎杀悬赏；`missionEvent` 增加 burn 防御。
4. **P2-4 基地休息冷却**（`constants.js`/`game.js`/`save.js`）：夜间休息增加 240 秒冷却（`BASE_REST_COOLDOWN`），冷却中不能连续跳夜，状态随存档保存。
5. **P2-5 太空战斗数组安全**（`spacecombat.js`）：微陨石/海盗弹伤害触发紧急返航时立即退出本帧，不再继续遍历已被 `clear()` 替换的数组。
6. **P2-6 创造模式任务板禁用**（`game.js`/`ui.js`）：接单、交付、自动进度在创造模式全部冻结，空间站任务板 UI 显示不可用。
7. **P3-7 海盗赏金反馈**（`spacecombat.js`）：满背包击毁海盗改为“背包已满，赏金无法接收”，不再显示 +0。
8. **P3-8 仓库卫生**：自然选择号模型草稿全部入库（`models/`、预览页与 bundle、`src/entities/naturalSelection.js`、4 个 tools 脚本）；文档口径统一为“38 套游戏回归 + 1 套模型校验”。

### 测试
- `tests/run.js`：**44677 通过，0 失败**（新增水体列完整性、pending 补发与存档、burn 事件隔离、基地休息冷却存档往返等断言）。
- 新增/扩展浏览器回归：
  - `tools/verifywater.mjs`：湖泊水面完整保留；
  - `tools/verifymilestones.mjs`：满包暂缓 + 腾位补发；
  - `tools/verifymissionboard.mjs`：真实 Mob 燃烧死亡不计入、创造模式任务板冻结；
  - `tools/verifybase.mjs`：连续休息被冷却阻止、清零后可再休息、读档恢复冷却；
  - `tools/verifyspacecombat.mjs`：敌弹致死立即返航且数组清空、满背包赏金文案；
  - `tools/verify-natural-selection.mjs`：模型几何校验全绿。
- 关键回归：`verifyfullrun` 主线 17/17 全过，`ERRORS: none`。

### 下一轮候选
- 基地深化：床铺视觉/基地命名/多基地列表；
- 自然选择号模型整合：预览模型可作为曙光号外观/空间站展示；
- 生物视觉与行为多样性：稀有精英怪/种群差异；
- 飞船武器升级：舰炮伤害/射速模块。

## 第 54 轮（已完成）：性能优化——帧率优先的动态分辨率与网格化调度

### 目标
低配/高分辨率设备上出现明显掉帧。本轮围绕“帧率优先”做热路径与调度优化，不改变画质档位语义。

### 内容
1. **动态分辨率**（`game.js`）：用真实 RAF 间隔（含合成/GPU 等待）做滑动平均；持续高于 38ms 时逐步降低渲染像素比（最低 0.6），低于 15ms 时逐步恢复；切换画质时重置。
2. **自适应网格化预算**（`world.js`/`game.js`）：帧余量充足时 remeshBudget 1→6，快速消化脏区块；帧慢时压回 1，保护帧时间。
3. **区块调度分层**（`world.js`）：新增 `edgeRefresh` 低优先级队列。高速移动/飞行时新区块只占高优先级 dirty，相邻旧区块边界刷新延后到预算宽裕，避免每秒新增数十区块时脏队列爆炸。
4. **减少热路径 GC**：
   - `sky.update`：所有逐帧 Color/Vector3 改为模块常量或实例缓存；
   - `player.updateTarget`：眼睛/视线向量复用；
   - `particles`：InstancedMesh 实例颜色缓冲区常驻，不再每次粒子爆发重新分配；
   - `game.loop` 坠机烟柱坐标复用；
   - `world.remeshQueue`：dirty 数组复用，不再每帧 `[...dirty]`。
5. **减少无意义 DOM 写入**（`ui.js`）：天气滤镜与两个罗盘的 classList/style/textContent 值未变化时直接跳过。
6. **测试工具**：`tools/perflow.mjs` 支持 `GAME_URL`/`CHROMIUM_PATH` 环境变量，并输出 `edgeRefresh` 与 `renderScale`。

### 验证
- `node tests/run.js`：**44677 通过，0 失败**。
- 浏览器回归：`verifywater` / `verifybase` / `verifymissionboard` / `verifyspacecombat` / `verifymilestones` / `verifyfullrun` 全绿，`ERRORS: none`。
- 无头软渲染运动基准：低画质 r4 平均约 397 FPS、中画质 r6 约 246 FPS、高画质 r6 约 537 FPS、高画质 r10 + 瞬移压力约 228 FPS。
- 自适应路径实测：模拟 60ms 帧后 `renderScale` 从 1 降至 0.8 且 remeshBudget 压回 1；模拟 8ms 帧后恢复至 1。

### 下一轮候选
- 基地深化：床铺视觉/基地命名/多基地列表；
- 自然选择号 V2 模型整合：预览模型可作为曙光号外观/空间站展示；
- 生物视觉与行为多样性：稀有精英怪/种群差异；
- 飞船武器升级：舰炮伤害/射速模块。


## 第 55 轮（进行中）：Universe V2 架构迁移——真实米制宇宙与连续地月样板链路

### 目标
按宇宙空间体系审计结论，替换“多个平面世界 + 本地太空布景 + 固定出生点传送”的 V1 底层，
建立独立于玩家的宇宙结构、统一尺度、三维矢量飞船运动、统一太阳光照与真实距离地月链路。

### 已落地
1. **新宇宙逻辑层** `src/space/universe.js`：
   - 恒星系统内真实米制坐标（AU_M / 光速 / 真实半径与轨道半长轴）；
   - 每颗可登陆天体固定地表锚点与 `surfaceToUniverse/universeToSurface` 双向映射；
   - 天体自转相位、恒星方向、脉冲速度包线均为纯函数，可在 Node 测试。
2. **飞船运动 V2** `src/entities/shipflight.js`：
   - `vel` 为真实三维速度矢量；W 沿机头（含 pitch）推进；S 沿速度反向制动；
   - 太空按真实速度积分；接近天体时由距离包线连续自动减速；
   - 地表帧 ↔ 宇宙帧速度/姿态连续换算。
3. **太空系统 V2** `src/space/systemV2.js`：
   - 天体渲染使用真实位置/真实半径 + 浮动原点；
   - 跨天体从“跃迁闪光 + 固定出生点”改为“接近 + 速度达标后连续转换”，落点由真实位置反算；
   - 空间站/小行星带/跃迁门接入 Universe 坐标；
   - 罗盘读取 Universe 并修正前后颠倒问题。
4. **统一太阳/昼夜**：
   - 地表太阳方向、太空方向光、星图方向全部由恒星位置计算；
   - 天空球着色器支持世界帧 up/sun/moon 方向与真实角直径；
   - 步行、大气层、太空三种状态共用 `setSkyForPosition`。
5. **星图与导航**：
   - 系统级星图投影玩家真实位置；距离读取 Universe；
   - 行星系统图按真实卫星方向与轨道比例绘制（径向做可读性压缩）；
   - HUD 脉冲速度自动切换 km/h / c。
6. **存档 V2**：
   - 保存飞船位置/速度/姿态/太空状态/宇宙坐标，读档恢复。
7. **兼容与测试**：
   - 旧 `system.js`/`celestial.js` 数据与导出保留，原 44677 项测试全绿；
   - 新增 `tests/run-v2.js`：32 项 Universe V2 纯逻辑与地月连续转换测试全绿；
   - `package.json` 新增 `npm run test:v2`。

### 下一阶段候选
- 轨道时钟（公转/卫星绕转）；
- 球面区块分页与跨 360° 地表连续；
- 气态巨行星/恒星/小行星差异化接近；
- 恒星级地图真实尺度；
- 浏览器回归套件 V2 适配。
